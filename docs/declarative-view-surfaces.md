# Declarative View Surfaces

This guide describes the current `viewSurfaces` authoring contract as of 0.33.5.18.15. The protected-surface inventory is current through the 0.33.15.8 Settings host closeout, and the field metadata/factory contract is updated through 0.33.14.2.

Declarative view surfaces are framework-rendered protected surfaces described by module manifest data or by framework-owned descriptor registries for framework-owned pages. They are for common app anatomy: page headers, filters, selector/index panels, split layouts, tables, detail headers, action strips, summary panels, field grids, modal shells, modal footers, item rows, and linked-record panels.

Modules still own business behavior. A module descriptor may name fields, routes, permissions, labels, and behavior IDs, but the module browser script or service still owns data meaning, validation, save payloads, API calls, permission enforcement, and workflow decisions.

As of 0.33.5.28.2, declarative view surfaces are also downstream of the completed agnostic database contract. Descriptors, browser adapters, and behavior handlers may request normalized routes and preserve UI state, but they must not express SQL, raw provider dialect, canonical paging/filtering rules, permission pruning, or cross-module table joins. Those remain server-side route/service/repository concerns built on `src/core/database.js`, named params, and the `db.dialect` seams.

## Authoring Steps

1. Declare a protected view in `protectedViews`.
2. Add a `viewSurfaces` descriptor bound to that view by `moduleId` and `viewId`.
3. Keep the protected HTML view as a minimal host that loads `view-builder.js`, `view-renderer.js`, and the module adapter script.
4. Expose a normalized data route and map response fields through `dataSource.fieldBindings`.
5. Put common anatomy in the descriptor: header, filters, index, table, detail, item rows, linked records, modals, and actions.
6. Register module-owned behavior handlers with `LongtailForge.view.registerBehavior(id, handler)` for actions that are not simple route actions.
7. Keep module-specific field binding, task pickers, catalog suggestions, payload construction, and permission-shaped workflow calls in the module adapter.
8. Add a focused regression before enabling strict guardrails for the surface.

## Layouts (0.33.5.18.2)

Supported `layout` values: `single-column`, `stacked`, `sidebar-detail`, `slide-out-sidebar`, and `table-page`. The `split-list-detail` layout is **retired** (the `createSplitListDetail` primitive and `.view-split-list-detail` CSS remain only as deprecated compatibility shims).

- `stacked` is the standard list/detail layout: a collapsible filters panel, a height-capped scrollable index panel on top, then a full-width detail panel below. Filters render collapsed by default.
- `slide-out-sidebar` is the preferred action/workflow surface anatomy when filters, libraries, or navigation need to sit beside a primary record or list surface without squeezing the main content.

## Shared Capabilities (0.33.5.18.1)

The 0.33.5.18 conversions use shared capabilities added during the conversion backlog:

- Filter-to-refetch: add `filters[]` with a `queryKey` (defaults to `field`). The framework appends non-empty filter values to the `dataSource` route, seeds the first load from `default`, and refetches when a filter changes. Authors do not write fetch or query-string code.
- Dynamic field options: select and search filters may declare `optionsSource` with a registered behavior ID. The framework renders the control, hydrates select choices or a body-level search suggestion popover, and calls the behavior with the control plus `setOptions(options)` / `mountSearchOptions(options, config)` helpers. The owning module supplies Client/Project/tag/etc. option data and keeps record ownership, query semantics, and whether typed search text, a selected label, or a canonical ID is submitted.
- Mount regions: add `regions[]` (or `detail.regions[]`) entries shaped `{ id, behavior, title }`. The framework renders the region container and calls the registered behavior with `{ container, record, api, refresh, openModal, workspaceContext }`. Use this to host module-owned widgets (tag pickers, file panels, Markdown preview, timers, checklists) instead of hand-building them in the page. A missing behavior renders a recoverable error.
- Rich item rows: `detail.itemRows` supports `itemTitleField`, `itemSubtitleField`, `chips`, `metaFields`, and `rowActions`. Gate row actions with `visibleWhen` (`equals` / `in` / `truthy` / `falsy`) against the row record; route actions interpolate `{token}` placeholders from that record.
- Display-only hierarchy metadata: `indexPanel.itemDepthField`, `indexPanel.itemParentField`, `indexPanel.itemPathField`, and `table.hierarchy.{depthField,parentField,pathField}` let flattened tree reads carry depth/parent/path values for safe display. The framework may indent or expose metadata; hierarchy mutation, validation, drag/drop, and save behavior stay module-owned.
- Table display hooks: table columns may use `formatter: "hierarchy-label"` for readable indented labels or `formatter: "chip-list"` with `chipsField` / `chipLabelField` for safe chip display. The module still owns readable values, tag assignment, billing meaning, permissions, and route calls.
- Table row selection shell: table descriptors may declare `selection` metadata so the renderer owns checkbox anatomy and stable row-selection hooks. `selection.headerLabel` may override the visible selection-column heading, including an explicit empty string when the checkboxes' own accessible names make that heading redundant. The module still owns selected-ID state, allowed bulk actions, payloads, confirmations, route calls, partial-failure handling, and refresh behavior.
- Table-page slide-out filters: `filterPlacement: "slide-out-sidebar"` moves a `table-page` surface's filters into the shared left-side drawer. The framework owns the trigger, drawer, backdrop, filter form placement, focus/ARIA state, and close behavior; modules still own option hydration and canonical filter semantics.
- Table secondary rows: table descriptors may declare `secondaryRows[]` entries for supporting record metadata such as tag chips. The framework owns the extra row/cell anatomy and responsive wrapping; modules still own which values are readable and what assignment behavior exists. `table.rowActionsHeaderLabel` may likewise override or intentionally suppress the visible action-column heading without removing accessible names from the row controls.
- Icon-aware actions: descriptor actions may declare `icon`, `iconOnly`, and `title` so dense or repeated action placements can use shared icon-only controls with accessible labels while the module keeps action meaning and behavior handlers.

Capabilities intentionally NOT yet in the descriptor (use an escape-hatch behavior, or wait for the surface that introduces them): editable hierarchy/tree interactions, descriptor-declared bulk action payloads, pagination/load-more, and general form-field `visibleWhen`. The imperative `LongtailForge.view` helpers and `renderDescriptor*` functions remain the escape hatch for genuinely custom fragments; do not use them to rebuild anatomy a descriptor already covers. As of 0.33.5.18.10.4, `LongtailForge.view.createBulkActionToolbar` provides a framework-owned collapsed shell for module-owned bulk-control bodies, `LongtailForge.view.createListShell` provides a framework-owned list wrapper/status mount for module-owned row bodies, `LongtailForge.view.createDetailActionStrip()` provides framework-owned placement for descriptor-backed Task row lifecycle actions, `LongtailForge.view.createDetailActionMenu()` provides framework-owned placement for descriptor-backed Task row workflow actions, `LongtailForge.view.createDetailBadgeRow()` provides framework-owned detail/read metadata and task context/relationship badge-row anatomy, `LongtailForge.view.createLinkedContextList()` provides framework-owned linked-context read-list anatomy for already-linked records, `renderDescriptorModalForm()` provides the framework-owned shell/footer/grid for the shared Tasks add/edit modal, `createModalForm()` plus the modal stack helpers provide the framework-owned recurrence child shell, and `LongtailForge.tasksDialog.openTaskEditor()` is the canonical browser opener for that modal. Tasks uses one framework-owned Task Details section, a framework-owned top summary badge row, framework-owned row context badge anatomy, and Notes-owned linked-note rows rendered through the shared linked-context read list while keeping row lifecycle visibility/handlers, assignment/scheduling/recurrence editor dispatch, timer route calls, non-destructive bulk status, priority, assignee, due-date, due-time, tag, archive, restore, relationship rules, task modal payloads, validation, primary-context rules, readable metadata values, recurrence state/rules, reminder overrides, checklist rows/actions/progress, timer state, tags, files, linked notes, copy-link, notification follow, callbacks, refresh hooks, and focus return behavior on module-owned route/service/browser paths. Tasks does not expose task delete controls because no shipped task delete workflow exists.

## Field Metadata And Editable Grids (0.33.14.2)

Descriptor modal, item-form, linked-record, filter, and persistent-grid rendering now share `LongtailForge.view.createField(field, options)`. Supported factory and `VIEW_FIELD_FIELDS` types are `text`, `number`, `select`, `multi-select`, `boolean`, `checkbox`, `toggle`, `switch`, `radio`, `textarea`, `date`, and `time`; the shipped `search`, `hidden`, and `url` compatibility types remain valid for existing descriptors. Metadata validated through `VIEW_FIELD_FIELDS` may include `max` and `inputmode` in addition to the existing `min`, `step`, `required`, `options`, `optionsSource`, `default`, `placeholder`, `rows`, `autocomplete`, `hidden`, and `width` keys. The narrower top-level filter descriptor schema keeps its existing keys even though its rendered control uses the same factory.

The factory owns label/control association, radio `fieldset`/`legend` anatomy, switch semantics, multi-select selection binding, stable `data-view-input` hooks, and a per-field message channel. Callers set or clear that channel through `viewParts.setMessage(...)`; the primitive synchronizes message placement, `aria-describedby`, and `aria-invalid`, while callers retain validation meaning and save decisions.

Persistent descriptor item forms remain read-only unless they explicitly declare `editable: true`. Imperative descriptor grids may pass `{ editable: true, values }` to bind enabled controls from a record-shaped value object. `LongtailForge.view.collectFieldValues(grid)` and `grid.viewParts.collectValues()` collect live typed values, omitting disabled controls by default. The current renderer, Reporting host, and shared Settings controls are the three real consumers; Settings groups those flat grid values back under their existing module IDs without moving setting validation or persistence into the view primitive.

Framework-owned field consumers must use `createField()` instead of raw `document.createElement` construction for covered field anatomy. Descriptors and module adapters still own field meaning, option hydration, validation, conditional visibility, save payloads, permissions, and routes.

As of 0.33.5.18.10.3, Tasks uses one framework-owned Task Details section and a framework-owned top summary badge row in the canonical modal while keeping row lifecycle visibility/handlers, assignment/scheduling/recurrence editor dispatch, timer route calls, non-destructive bulk status, priority, assignee, due-date, due-time, tag, archive, restore, task modal payloads, validation, primary-context rules, readable metadata values, recurrence state/rules, reminder overrides, checklist rows/actions/progress, timer state, tags, files, linked notes, copy-link, notification follow, callbacks, refresh hooks, and focus return behavior on module-owned route/service/browser paths.

## Guardrails

Strict guardrails currently enforce `client-projects.clients`, `client-projects.projects`, `files.browse`, `lists.workspace`, `notes.workspace`, and `tasks.workspace`. Lists now uses one declarative linked-record path: `List Details` renders read-only rows through `createLinkedContextList(...)`, and the Create/Edit List modal owns the only write path through `createLinkedContextPicker(...)`; strict Lists guardrails fail if the retired detail-side task/raw-ID linked-record editor or its task bootstrap fallback returns. Tasks declares `tasks.workspace` for the protected page shell/read path, with ordered sidebar panels for a non-collapsible saved task view selector and collapsed Sorting and Filters controls. The main task list is bound through the `tasks-main-list` detail region, and the list shell, bulk toolbar, row lifecycle action strip, row workflow action menu, add/edit modal shell, top detail/read metadata badge row, recurrence child-modal shell, Checklist section shell, Task Timer section shell, utility footer/heading placement, and stacked Tags/Files utility child dialogs use shared framework helpers. Strict Tasks guardrails now fail if Tasks reintroduces raw template parsing, protected-page anatomy, modal shell/footer construction, standard field-grid markup strings, or standard action placement outside descriptor/rendered helpers. Tasks still owns selected-view query behavior, row data shaping, lifecycle and workflow behavior handlers, row utility actions, metadata value selection, recurrence/reminder/checklist/timer behavior, tags/files/notes/copy-link/notification behavior, bulk semantics, relationship rules, payloads, validation, and dialog workflows as documented escape hatches in `docs/tasks-strict-guardrail-inventory.md`. The 0.33.5.18.10.7 closeout also locks `LongtailForge.tasksDialog.openTaskEditor()` as the canonical module-owned Task editor opener for Tasks page, Workbench, future Quick Action Center, and future module-triggered task creation flows. Tags and Developer Example descriptors are inventoried and delivered through the descriptor pipeline, but their protected views are not strict-converted surfaces in this closeout. Files now exposes the framework-owned `files.browse` descriptor as a strict surface; its protected host is minimal, its filter sidebar mounts through `files.browse.filters`, and its results region mounts a shared list shell plus data table through `files.browse.results` while Files keeps route calls, query semantics, row shaping, readable-label fallbacks, safe file badge/truncation tooltip data, route-backed availability, workspace-specific Client visibility, and download/delete/restore actions. Files also exposes the Files-owned `LongtailForge.filesDialog.openFileEditor()` shell for File Context editing through shared modal/form/footer helpers, read-only metadata rows, stable hierarchy-ordered Client/Project controls, provider-backed cross-module target choices such as Tasks and Notes, persisted-row click/Enter open behavior, route-backed context Save, and the same standalone Preview action as the Files list for previewable attachments. The preview availability contract is `GET /api/files/attachments/:fileAttachmentId/preview`, which checks the selected attachment target and context-scoped Files permission before returning safe state/kind metadata. Previewable descriptors include a route-backed `/preview/content` URL for authenticated image, text, and Markdown content, and the Files browse row Preview button opens a dedicated route-backed Preview modal. Image content streams through Files routes, text/Markdown content returns capped JSON, and Markdown rendering uses the shared server Markdown service without raw HTML or unsafe links/images. Files browse intentionally does not render inline Browse Summary, persistent Preview panes, Metadata panels, selected-file detail, or selected-row state; row click opens File Context, Preview opens the Preview modal, Download downloads, and Delete/Restore mutate lifecycle. As of the 0.33.5.18.11.13 closeout, Files remains a compact workspace file recovery surface with Inspector integration deferred; 0.33.5.18.12 may standardize upload, attachment-panel, and action placement, but it should not reimplement File Context or Preview routes/modals. As of 0.33.5.18.12.1, the shared Files attachment helper standardizes upload/dropzone shell, accepted-file hint, upload status/action, and per-file result placement through shared helpers while preserving Files-owned upload behavior. As of 0.33.5.18.12.2, the shared Files attachment helper also standardizes its reusable attachment panel, list, empty/status states, upload-result rows, and dense attachment action shell through shared helpers while keeping attachment route behavior Files-owned and avoiding inline File Context, Preview, Metadata, or Inspector bodies. As of 0.33.5.18.12.3, Files row and attachment-panel controls use shared action IDs for Preview, Download, Report, Quarantine, Remove Attachment, Delete, and Restore while Files-owned browser code keeps route calls, confirmations, refresh behavior, permission-shaped visibility, and service-owned lifecycle authority. As of 0.33.5.18.12.4, Files and attachment-panel visual states use wrapping status/review chips, broad recovery/review language, and mixed icon/text action parity while preserving Files-owned routes, permissions, File Context, Preview, and compact listing boundaries. As of 0.33.5.18.12.5, `docs/files-strict-guardrail-inventory.md` maps the remaining Files framework-owned candidates and Files-owned escape hatches. As of 0.33.5.18.12.6, Files strict guardrails enforce that `files.browse` keeps protected-page, filter, list/table, attachment-panel, upload, dense-action, and modal shell anatomy in descriptors/shared helpers while allowing Files-owned route calls, file reads, upload payloads, availability, target context, confirmations, permission-shaped visibility, recovery states, File Context, and Preview behavior. (Notes mounts a secondary Library navigation panel via the framework `createCollapsibleIndexPanel` primitive - an allowed exception until the descriptor can express a second nav panel.)

As of the 0.33.5.18.12.7 closeout, the Files conversion branch is complete at this boundary. `files.browse` stays strict; upload, attachment-panel, row/attachment action, visual-state, File Context, and Preview anatomy all use descriptors or shared helpers where framework-owned; and File Context plus Preview remain route-backed modal workflows rather than inline browse bodies. The current Files review-flow exception keeps `Mark Reviewed` inside the route-backed File Context modal and uses the existing Files restore route. It does not permit inline browse review panes, raw scan/quarantine metadata controls, storage controls, or general Inspector behavior.

As of 0.33.5.18.14.3, `docs/clients-projects-strict-guardrail-inventory.md` maps the Clients/Projects page conversion boundary before strict enforcement. `client-projects.clients` and `client-projects.projects` are active descriptors with framework-rendered read anatomy while remaining reporting-only; the protected hosts are minimal but not strict. The browser adapter registers descriptor Add/Edit and bulk-region behaviors that route through the shared module-action registry or `LongtailForge.view.createBulkActionToolbar()`, hydrates tag and Client filter options, preserves query-param openers through the same canonical action IDs, hides Client filters and Project Client reassignment outside Business workspaces, renders related Project and Project-context read rows through shared list/table/action helpers, and refreshes the descriptor surface after existing dialog or bulk saves. Clients/Projects keeps hierarchy rules, related-row shaping, selected IDs, allowed bulk actions, billing/status/Client payloads, partial-failure messaging, billing/task-default editors, tag assignment, route calls, permissions, save payloads, dialog bodies, and workflow behavior.

As of 0.33.5.18.14.4, the Projects read page still binds to `/api/projects?include_depth=true`, and `/api/projects` now supplies service-owned workspace-first Project ordering before Client-backed Project groups in readable Client hierarchy order. The registered edit behaviors remain the move/reparent entry points because they open the existing Client/Project editors and submit existing validated payloads. The descriptor renderer may render hierarchy metadata and dispatch behavior IDs, but it must not own Project grouping, parent validation, cycle prevention, same-scope checks, archived-parent checks, or drag/drop hierarchy editing.

As of 0.33.5.18.14.5, `client-projects.clients` and `client-projects.projects` strict enforcement is active. The framework owns the converted page/read anatomy: minimal hosts, page headers, status/empty/loading/error shells, table-page slide-out filter surface, selector/table wrappers, row-selection checkbox shell, secondary tag rows, icon-only repeated edit controls, related read shells, bulk toolbar shell placement, and descriptor behavior dispatch. Clients/Projects owns tag and Client filter option hydration, selected IDs, allowed bulk controls, route calls, payloads, confirmations, partial-failure messages, dialog bodies, tag pickers, billing/default editors, parent selectors, hierarchy validation, readable labels, query-param openers, refresh hooks, and workspace gating. Strict guardrails fail if inline top filters, standalone Tags table columns, text repeated table actions, protected page anatomy, legacy page table chrome, or static page bulk-dialog shells return. This slice does not change database schema, route payloads, permissions, or workflow behavior.

As of 0.33.5.18.15, the 0.33.5.18 view-conversion branch is closed. Strict fail-on-violation guardrails cover `lists.workspace`, `notes.workspace`, `tasks.workspace`, `files.browse`, `client-projects.clients`, and `client-projects.projects`. Tags management and Developer Example descriptors remain reported descriptor proofs. Reporting uses the separate catalog-driven narrow framework host shipped in 0.33.12.6 rather than a `viewSurfaces` descriptor, and 0.33.12.7 closes its strict guardrail inventory. Admin/Settings, pagination/server-side paging, Inspector behavior, and unrelated non-view concerns remain deferred to later roadmap lines. The closeout does not add database schema, write payload, permission, public API, or new workflow changes.

As of 0.33.12.7, the Reporting host is strict. `views/protected/reporting.html` contains one shared-width mount, and `public/js/reporting.js` composes the page through `LongtailForge.view`; direct element creation is limited to the permission-filtered renderer `link` and `script` assets returned by the catalog. Static guardrails reject hard-coded framework page anatomy, Reporting-only layout/footer classes, and Time Tracking report IDs, renderer paths, service imports, or result-shape knowledge in framework Reporting HTML/browser/service code.

A strict declarative surface must:

- Keep protected HTML to a minimal host.
- Use descriptor data for framework-owned anatomy.
- Avoid direct module calls to `LongtailForge.view` primitives for page headers, filters, split layouts, tables, action strips, field grids, modal forms, or inline action rows.
- Avoid `document.createElement("dialog")`, `document.createElement("table")`, and `document.createElement("details")` in the module adapter for framework-owned anatomy.
- Avoid one-off layout/footer classes when a descriptor field or framework class exists.
- Keep business behavior module-owned: routes, payloads, validation, permissions, task pickers, catalog suggestions, and workflow decisions.

## Minimal Host

The protected HTML page should provide only document metadata, shared assets, a host element, and scripts. Lists is the current strict example:

```html
<main class="wide-page lists-page" data-lists-host></main>
<script src="js/shared/view-builder.js"></script>
<script src="js/shared/view-renderer.js"></script>
<script src="js/lists.js"></script>
```

The module adapter decorates descriptor-rendered nodes with compatibility hooks only where existing data binding still needs them.

## Protected View Inventory

The inventory below is current through the 0.33.12.7 Reporting closeout. `strict` means a static guardrail fails on the relevant framework-owned host/surface violations. `reported` means the view is known to the inventory but is not strict-converted. Files is strict as of 0.33.5.18.12.6 after the strict guardrail inventory was promoted to enforcement. Clients/Projects strict enforcement is active as of 0.33.5.18.14.5 after the filter drawer, secondary tag-row, icon-only table action, and legacy page chrome cleanup. Reporting is strict through its catalog-driven framework host rather than a `viewSurfaces` descriptor.

| Module | View | File | Descriptor Surface | Guardrail |
| --- | --- | --- | --- | --- |
| API Keys | api-keys | api-keys.html | - | reported |
| Audit Log | audit-log | audit-log.html | - | reported |
| Calendar | calendar | calendar.html | framework-built read-only host | strict |
| Client Projects | clients | clients.html | client-projects.clients | strict |
| Client Projects | projects | projects.html | client-projects.projects | strict |
| Dashboard | dashboard | dashboard.html | framework-built contribution host | strict |
| Developer Example | developer-example | developer-example.html | developer-example.surface | reported |
| Files | files | files.html | files.browse | strict |
| Files Settings | files-settings | files-settings.html | framework-built settings host | strict |
| Help | help | help.html | - | reported |
| Lists | lists | lists.html | lists.workspace | strict |
| Notes | notes | notes.html | notes.workspace | strict |
| Notifications | notifications | notifications.html | - | reported |
| Reporting | reporting | reporting.html | framework-built catalog host | strict |
| Search | search | search.html | - | reported |
| Tags | tags | tags.html | tags.management | reported |
| Tasks | tasks | tasks.html | tasks.workspace | strict |
| Tasks Settings | tasks-settings | tasks-settings.html | framework-built settings host | strict |
| Time Entries | time-entries | time-entries.html | - | reported |
| Time Tracker | time-tracker | time-tracker.html | - | reported |
| Time Tracking Settings | time-tracking-settings | time-tracking-settings.html | framework-built settings host | strict |
| User Admin | user-admin | user-admin.html | - | reported |
| User Settings | user-settings | user-settings.html | framework-built settings host | strict |
| Workbench | workbench | workbench.html | framework-built guided host | strict |
| Workspace Settings | workspace-settings | workspace-settings.html | framework-built settings host | strict |

Settings hosts are strict through the shared Settings contract rather than through `viewSurfaces` descriptors. Workspace, User, Tasks, Time Tracking, Files, and Developer Example Settings protected HTML must stay minimal `data-settings-host` mounts that load `view-builder.js`, `settings-renderer.js`, and `settings-host.js` in order. `LongtailForge.settingsHost` owns page/header/section/action/status/modal anatomy, `LongtailForge.settingsRenderer` owns contribution field rendering, optional visual grouping, and typed collection, and page adapters keep their owning route calls, operational readouts, and domain saves. A Settings host or renderer must not name first-party module setting IDs to decide field behavior; Workspace's explicit client-projects/optional-module placement split is the framework-owned information-architecture exception.

As of 0.33.6.13a, Dashboard no longer carries static protected-page panel anatomy in `views/protected/dashboard.html`; the host is a minimal `data-dashboard-host` shell. The browser adapter builds the Dashboard page header, status message, Workspace Pulse, setup-warning region, framework-owned dashboard regions, empty state, and host-owned Project/Task panel renderers through `LongtailForge.view` primitives while loading the framework `/api/dashboard` read model from `src/services/dashboard.service.js`. Panel placement is driven by the validated contribution `placement` field and defaults to `main`; the browser must not special-case first-party panel IDs such as `project-summary` to choose placement. Module-specific data hydrates through module-owned `dataRoute` endpoints such as `/api/client-projects/dashboard/project-summary`, `/api/tasks/dashboard-summary`, and `/api/time-tracking/dashboard/effort-summary`. As of 0.33.6.13b, Tasks declares three Dashboard panels over that same route: `tasks.needs-attention` in `attention`, `tasks.today-upcoming` in `today`, and `tasks.pressure` in `main`; the browser renders the service-shaped `attentionRows`, `upcomingRows`, `pressureRows`, `metrics`, and `actions` without rebuilding task pressure logic or linking task rows into the edit modal. As of 0.33.6.13c, Time Tracking declares compact `active-timers` and `recent-time` Dashboard panels while the retired `current-month-billables` and `hours-billables-chart` billing panels stay out of default Dashboard placement. As of 0.33.6.13d, the `main` region is styled as the Module Overview grid, Time Tracking effort cards live there with the compact Tasks pressure card, and the `activity` region renders a quiet deferred Recent Activity state until a safe digest exists. As of 0.33.6.14.1, Dashboard host guardrails are strict for this branch: the hidden `data-dashboard-extension-panels` stub, static protected-page billing markup, host-owned module payloads, ID-based placement logic, fake future-module cards, unsafe activity rows, default Dashboard billing tables/charts, full task indexes, and inline editor/form patterns must not return.

As of 0.33.6.12d-1, Workbench no longer carries static protected-page anatomy in `views/protected/workbench.html`; the host is a minimal `data-workbench-host` shell. The browser adapter builds the page header, header-slot status message, `Change Focus` state action, guided focus questions, split client/project focus filters, one recommended-action card, useful empty states, a right-side "More in this focus" Inspector overflow panel, an active/paused-only Focus Selection Timers card, and the Task Focus selected-task sections through `LongtailForge.view` primitives while keeping source routes, candidate data, deterministic ranking, permission/workspace/module scoping, timer/task actions, focus persistence, and module-owned record semantics outside the framework helpers. The recommended-action cycle window is the top five ranked candidates, and the Inspector overflow begins after that window so the active/top-five recommendation set is not duplicated in the right panel. Pick up where I left off executes the focus resolver's resume strategy by reading existing resume-state candidates first, ranking running timers, paused timers, resume-note tasks, and In Progress tasks ahead of weaker candidates, and falling back to ranked candidate buckets only when no resume rows are available. Resume-backed candidates no longer expose a `Dismiss` action; blocked, stale, or alternate work is represented through task status and focus selection. Normalized Task candidate primary actions are labeled `Focus task` and enter `task-focus` by recording the active task focus instead of opening the Task edit modal. Focus Selection Inspector titles choose/focus the candidate through the same primary-action path, while Task Focus Inspector consumes the selected-task related-context route. Non-task primary candidates keep an explicit temporary page fallback through their safe source URL until their owning module has a Task Focus-equivalent target view. The removed all-tasks Workbench list, task filter bar, task sort control, task-list actions, `taskItems` browser state, main-column "More in this focus" section, manual timer creation row, and empty bootstrap `taskItems` compatibility field must not return. The focus resolver feeds due modes through the shared `due_datetime` candidate sort, keeps Review blocked work scoped to genuinely blocked candidates only, and now applies the split filters through the shared descendant-aware hierarchy resolver so parent client/project selection includes readable descendants while leaf selection stays narrow. Focus Selection Timers auto-opens only when active or paused timers exist unless manually toggled in the current session, and Workbench collapsible summaries expose a consistent caret/focus affordance over native `<summary>` semantics. As of 0.33.6.14.1, Workbench host guardrails are strict for this branch: Task Focus must keep Focus Selection panels hidden, task candidates must enter Task Focus first, explicit Edit actions must use canonical module-action openers, and the Inspector must not become an embedded preview pane.

As of 0.33.6.12c-1, Workbench Task Focus hides Focus Selection panels and renders a selected-task action strip, read-only summary, and collapsed read-only Task Details disclosure. The surface reads focused task details through the Tasks read route and dispatches Edit, Complete, and Block through existing Tasks-owned modal/lifecycle routes; it does not add task timer controls or selected-task Inspector context rows until their later slices.

As of 0.33.6.12c-2, Workbench Task Focus adds a check-only Checklist execution section after read-only Task Details. It uses the Tasks-owned check/uncheck routes and refreshed task payload while keeping checklist add, remove, rename, and reorder behavior in the canonical Task editor. As of 0.33.6.12l, that returned payload also carries Tasks-owned checklist-driven status transitions so Open becomes In Progress when checklist work starts and eligible In Progress returns to Open when all checklist work is cleared. As of 0.33.6.12m, Task Focus linked Note rows use the Notes-owned `notes.view` action for a rendered Markdown read modal before an explicit Edit handoff. As of 0.33.6.12n, recurring linked notes propagated through `All Future` surface through the same safe Task Focus related-context rows and still do not inline note previews in the Inspector.

As of 0.33.6.12d-1, Workbench Task Focus adds a default-open, collapsible Task Timer section after Checklist. It uses the focused task context automatically and exposes Start, Pause, Save Time, and Reset controls through the Tasks timer routes. Focus Selection keeps the active/paused timer list and no longer owns timer creation. As of 0.33.6.12d-2, QAC Timer dispatches the Time Tracking-owned `time-tracking.timer.create` modal instead of using the temporary Time Tracker page fallback. As of 0.33.6.12k, Task Focus keeps exactly one visible focused-task timer representation in the Task Timer section; its lower `Other Active Timers` panel excludes the focused task's timer and shows only other running/paused task or manual timers.

As of 0.33.6.12e-1, Workbench Task Focus has a selected-task related-context route and service read model. It aggregates linked Notes, task Files, linked Lists, same-project active Tasks, and direct shared-tag records through owning services, deduplicates strongest reasons, and exposes safe module-action descriptors for the later Inspector UI slice. Browser rendering is unchanged until 0.33.6.12e-2.

As of 0.33.6.12e-2, Workbench Task Focus renders that selected-task related context in the right-side Inspector. The browser fetches the route after the focused task read succeeds, groups rows by the service-provided groups, shows source/reason labels and badges, defaults the Inspector open, supports a caret collapse that preserves the side panel, and dispatches existing module actions or safe fallbacks without adding an embedded preview pane.

As of 0.33.10.2, Calendar is a framework-owned read-only host in the Dashboard/Workbench style: `views/protected/calendar.html` is a minimal `data-calendar-host` shell and `public/js/calendar.js` builds the page header, status message, period label, month/week/day segmented view switch, day grid, and empty state through `LongtailForge.view` primitives plus the framework `.calendar-*` CSS section. The surface consumes only the bounded, permission-shaped task calendar-window read (`GET /api/tasks/calendar`), renders task due entries with priority/status affordances and per-day reminder indicators from the payload's `reminders` markers, and opens entries through the canonical `LongtailForge.tasksDialog.openTaskEditor()` opener instead of any inline editor. Loading/error/disabled-module states run through the shared status-message anatomy and the period-empty state uses `createEmptyState`. The host intentionally has no calendar event record type, iCal, or external-sync behavior (0.36.0 / 0.70.x own those); guardrails/regressions close out in 0.33.10.4.

As of 0.33.10.4, the Calendar host is guardrailed and the 0.33.10 calendar branch is closed at this boundary. The `views.calendar-host` static guardrail fails if the protected page stops being a minimal host, if the adapter hand-builds framework-owned page/header/filter/status anatomy instead of using view primitives, if entries stop opening through the canonical Task editor opener, if the surface fetches anything beyond the bounded calendar window and filter options or adds mutating methods, if calendar event records/iCal/external-provider references appear in the surface or schema (0.36.0 owns events/iCal; 0.70.x owns Google/Outlook sync), or if Workbench grows beyond its lightweight week-view link. The `tasks.task-calendar-window` isolated regression proves bounded-range enforcement, workspace and permission scoping (no cross-workspace or unreadable-task leaks in rows or reminder markers), reminder-marker correctness (default date-only/date-time timing, lookahead, completed/archived exclusion, ordering), client/project filter scoping, and disabled-module read behavior. Year view, calendar events, iCal, and shared-calendar display remain 0.36.0 work.

As of 0.33.10.3, the Calendar surface has filters, period navigation, and framework navigation. A collapsed shared filter panel offers Client (Business workspaces only, hidden elsewhere) and Project selects hydrated from `/api/client-projects` through the shared `clientProjectOptions` helper, mirroring the Files/Tasks filter behavior; the server applies the shared descendant-aware filter scope before permission filtering. Previous/Today/Next period controls and the month/week/day switch re-query the bounded window, and `calendar.html?view=` / `?date=` query parameters select the initial view and anchor. The Tasks module contributes the permission-aware `Calendar` navigation entry (`requiredPermissions: ["tasks.view"]`, module-enabled gated) which the framework places in the Actions menu after Tasks, and Workbench's focus panel carries a lightweight "See this week on the calendar" link to `calendar.html?view=week` that is hidden when the navigation entry is not present — Workbench does not duplicate any calendar logic.

As of 0.33.10.6, the Calendar render path is shared instead of host-owned: `public/js/shared/task-calendar.js` publishes frozen `LongtailForge.taskCalendar` helpers owning the period-range math, the bounded `/api/tasks/calendar` window fetch, and month/week/day grid rendering through `LongtailForge.view` primitives. `public/js/calendar.js` keeps only the page chrome (header, status, toolbar, filters, view switch) and delegates the body to those helpers, and the Dashboard embeds a read-only month view below Workspace Pulse and Needs Attention through the Tasks `tasks-calendar` contribution in a framework-owned `calendar` region — same grid anatomy and mobile stacking, entries opening through the canonical Task editor, plus an `Open full calendar` handoff. The same slice fixed hidden status boxes at the root: `.view-status-message[hidden] { display: none; }` restores the `hidden` attribute that `.surface-main-panel`'s `display: grid` defeated, replacing the earlier Calendar-only rule, so the Dashboard no longer paints an empty persistent message box. The embed carries a today-anchored month/week/day segmented view switch (period navigation stays on the Calendar page). The same slice also retired the Dashboard `reporting` region with its Client/Project `project-summary` reporting-shortcuts panel, dedicated data route, and service read (an early-Dashboard relic; the Reporting page is unaffected), and Dashboard panels now suppress an inner panel title that merely repeats the region heading — the panel keeps an `aria-label` while distinct Module Overview card titles still render. The `views.dashboard-calendar-embed` guardrail pins the region order, the gated contribution, the shared render path (no duplicated grid logic, no direct window fetch from the Dashboard), the view switch, the retired reporting region staying retired, and the hidden-status anatomy; `views.calendar-host` extends to the shared helper file (primitives-only rendering, read-only fetches, no event/iCal/sync references).

As of 0.33.10.9, the Workbench calendar entry point is an icon-only button instead of the inline text link: it sits in the top-right of the "What should we focus on?" panel heading (a `workbench-focus-heading-row` two-column grid mirroring the recommended-panel heading), uses the shared `calendar` icon on a `button-link icon-button` anchor with the accessible "See this week on the calendar" name, still targets `calendar.html?view=week`, and keeps the navigation-tree gating (with a root `.button-link[hidden] { display: none; }` rule so the gated hidden state cannot ghost-paint — the same rule family as `.view-status-message[hidden]` and `.action-button[hidden]`). The `views.calendar-host` guardrail pins the icon-button anatomy, accessible name, heading placement, gating, and hidden rule. This closes the 0.33.10 follow-up slices; the branch is archived and the live roadmap continues with the cleanup sweep.

As of 0.33.10.8, the task editor offers an icon-only "Open in Workbench" handoff in the modal heading, immediately left of the notification bell inside a shared `.surface-modal-heading-actions` group. It uses the shared `anvil` icon, carries the accessible name via `aria-label`/`title` with no visible text, renders only when editing a persisted task (never on the create or duplicate dialogs), and navigates through the canonical `workbench.html?taskId=` deep-link contract so a stale or unreadable task degrades safely on the Workbench side. The same slice fixed hidden action buttons at the root (`.action-button[hidden] { display: none; }` — `.action-button`'s `display: inline-flex` had been defeating the `hidden` attribute, ghost-painting hidden modal utility toggles) and loaded `js/shared/notification-subscriptions.js` on the Workbench, Calendar, and Dashboard hosts, where the task editor's notification bell had been silently non-functional behind that ghost paint. The `tasks.task-editor-workbench-handoff` guardrail pins the icon, placement, icon-only accessibility, edit-mode-only visibility, deep-link parameter, the hidden-action-button CSS rule, and a generalized sweep that every protected view loading `js/task-dialog.js` also loads the subscription helper. The same slice made the Workbench "Recommended Next Action" card identifier-safe and context-rich: its meta line prefers client/project names resolved from the loaded workbench clients (so identically-titled tasks are distinguishable), title/meta/reason copy pass through the id-safe helper, `looksLikeRawId` rejects copy with embedded identifiers (hardening the Inspector too), and the shared server event summaries never fall back to `record_id` as a label — a label-less event summarizes as "Timer Paused." and that is what resume-state context snapshots persist. The recommended-cycling regression pins all of it.

As of 0.33.10.7, Workbench has a task-focus deep-link contract: `workbench.html?taskId=<id>` enters Task Focus for a readable task directly through the existing `enterTaskFocus` path (reusing a matching loaded candidate for context when one exists) as part of the normal Workbench load. Every failure — unknown id, unreadable or cross-workspace task, or a disabled Tasks module — falls back to Focus Selection with one shared generic status message that never echoes the requested id or distinguishes the failure reasons, so the link reveals nothing about whether a task exists. The Dashboard's per-task "Open Workbench" row actions deep-link to their row's task through the Tasks-owned dashboard-summary route (`workbench.html?taskId=...` built server-side), while the Workspace Pulse primary action and panel-level Open Workbench actions stay the generic Workbench entry. The `workbench.task-focus-deep-link` guardrail pins the parameter, the reuse of the existing focus path (no parallel fetch), the single no-leak fallback message, and the row/panel action split; the permission harness proves scoped users receive per-row deep links only for tasks they can read.

As of 0.33.6.12f, the Pick up where I left off focus boosts the second-most-recent updated readable active task from the Tasks Workbench item source after running/paused timer resume rows. The boost uses canonical task `updated_at`, respects Client/Project filters and source permissions, deduplicates against existing resume rows, and does not fabricate a candidate when fewer than two eligible tasks are in scope.
