# Module Manifest Contract

Longtail Forge modules are repository-owned static ESM entries discovered at build/development time from `src/modules/*/module.js`. A deterministic tracked catalog supplies those entries to `src/core/modules/registry.js`; the registry engine does not name workflow modules. The manifest shape is validated before database startup or runtime activation, and core app-shell/module behavior flows through the registry service. Third-party and operator-writable runtime loading remain intentionally deferred.

For implementation guidance, see `docs/module-development.md`. The disabled-by-default `developer-example` module is a working first-party stub that demonstrates route, page, setting, permission, event, notification, tag, and search declarations without adding business functionality.

## Startup Validation

Each `module.js` exports one canonical `moduleEntry` created with `createModuleEntry({ manifest, activateApp?, activateWorker? })`. Every bundled entry begins with `// @ts-check` and annotates its declaration as `ModuleManifest`, so `npm run typecheck` rejects structural drift in the fast gate before runtime validation. Importing that entry must only declare the manifest and activation functions; it must not register indexers, report runners, settings behavior, job handlers, startup sweeps, or other executable behavior. The module registry validates the complete generated catalog before exposing route, migration, metadata, or activation lists. Startup fails before database changes when the generated catalog is stale, a directory and manifest ID disagree, the canonical export is missing, an ID is duplicated, a manifest is invalid, a dependency is unresolved, or the dependency graph cycles.

Activation is synchronous and explicit. App and separate-worker activation follow validated module dependencies, with module ID as the stable tie-breaker. Module hooks may register executable behavior and module-owned startup tasks through the supplied activation context; framework app/worker bootstrap invokes only the generic module runtime. Run `npm run modules:registry:generate` after adding or removing a first-party `module.js`, and keep `npm run modules:registry:check` green.

Unknown arbitrary manifest fields are rejected. Future extension data should wait for a deliberate extension namespace instead of being added ad hoc.

The runtime contract remains one composed module definition per registered module. As of 0.33.18.5, Tasks and Notes compose substantial concern definitions from convention-aligned files while retaining `module.js` as their only canonical registry entry. Concern exports are side-effect-free data, not additional runtime entries; the composition assigns every manifest field explicitly and passes the unchanged complete-graph validator. Composition is a reviewability option for a roughly 500-line or otherwise difficult-to-review entry, not mandatory structure: add a concern file only when it owns substantial cohesive content, preserve IDs/routes/permissions/dependencies/contribution array order/activation behavior, and do not create empty files for small modules. The complete thresholds and example live in `docs/module-development.md`.

New manifest fields, registries, contribution types, and generalized module facilities follow the Two-Module Rule: normally identify two real first-party consumers with materially similar behavior and contract needs. A hypothetical consumer or shared appearance is insufficient. Keep a one-module requirement module-owned until the common contract is understood. Intrinsically framework-wide authentication, security, permission, workspace, deployment, database, and app-shell needs are explicit documented exceptions. Apply this prospectively and name the two consumers or exception at closeout.

As of 0.33.22.9.2, the private calendar-feed seam remains an explicit authentication/security exception. `src/core/private-feeds/private-feed-providers.js` accepts executable providers by stable lowercase ID during normal module app activation; provider functions do not enter the data-only manifest. Framework code owns named creator-bound credential records, the protected Admin Calendar view, hash-only creation/rotation, safe workspace metadata, revocation and reconciliation, exact entitlement authentication, throttling, generic rejection, HTTP serving, and dispatch. The Calendar destination is framework-owned and cannot be disabled or overridden; it sits under Settings → Admin → Modules without inventing a Calendar manifest solely for navigation. Dispatch includes a validated deep-frozen descriptor containing only subscription/name, owner/workspace identity, and normalized scope—never a URL, selector, digest, or Tasks row. Tasks registers the initial `tasks.calendar` provider, independently reauthorizes the descriptor, and owns all Workspace/Client/Project candidate, recurrence, permission-intersection, cancellation, and RFC 5545 behavior. This exception does not authorize framework reads of Tasks tables, arbitrary runtime providers, or a second feed type without a real consumer and reviewed contract.

## HTTP Route Contract

As of 0.33.18.2, framework and module routers run on Express 5.2.1. Route strings should use literal segments and named `:parameters`. Express 5 wildcards must be named: use `/*name` when the root path must not match and `/{*name}` when it must. Do not use the removed bare `*`, string-pattern `?`, character classes, regular-expression-like groups, or other reserved route-string metacharacters; use explicit routes or an actual reviewed `RegExp` route when that behavior is genuinely required.

Register asynchronous route work through the shared `asyncRoute` boundary unless a framework slice deliberately adopts native Express 5 forwarding for that router. Both paths must reach the final framework error middleware exactly once, must not write a second response after failure, and must preserve safe `AppError` shaping. Module routers must not install their own global body parser, query parser, trust-proxy policy, static fallback, not-found handler, or terminal error middleware. The framework explicitly preserves the extended query-parser shape; route/service contracts still validate and normalize every untrusted value. JSON, multipart, streamed upload, and download bodies remain owned by their existing bounded helpers and services.

The shared response envelopes, registered status codes, non-enumeration rules, request-ID support workflow, and new-route/browser-entry checklist are governed by [http-errors.md](http-errors.md). Module routes use `AppError` for expected failures and never emit raw production diagnostics.

## Registry Service

`src/core/modules/registry.js` is the synchronous registry engine. Its only bundled input is `bundled-module-catalog.generated.js`; source-directory discovery belongs to the generator/check, while startup also confirms that the tracked catalog matches repository-owned module directories. The database never supplies executable module paths, and arbitrary runtime directories are not loaded.

`src/core/modules/modules.service.js` is the framework-facing registry service. It provides module lookup, route lists, migration sources, enabled workspace module state, navigation/settings contribution collection, permission and API scope collection, audit record type collection, event type and event hook collection, event summary collection, taggable type lists, searchable type lists, Linked Context provider lists, notification contribution lists, Reporting report collection, and Workbench/timer/work-item contribution collection.

As of 0.33.32.12, that service is a `// @ts-check` consumer whose module and workspace projections derive from `ModuleManifest`; API-scope and executable event-hook paths use explicit catalog projections, database helpers accept the callback-scoped `TransactionClient`, and terminology resolution preserves the validated input shape. These are development-time boundaries only. Permission filtering, enablement and dependency/capability checks, deterministic ordering, workspace-context cache behavior, public envelopes, and lifecycle execution remain runtime-owned and unchanged.

Workspace-aware contribution helpers filter disabled modules, missing module dependencies, workspace capability mismatches, and user permission mismatches. Capability lists are treated as "any of these capabilities makes the contribution relevant" so Business, Personal, and Family workspaces can share module manifests without duplicate definitions.

As of 0.33.5.18.6.9.2, Tasks, Notes, and Lists are documented first-party workflow modules with module-owned storage behavior and workspace module-status settings. Tasks contributes task storage, recurrence, reminders, lightweight checklists, parent/child blocking relationships, task timer source routes, searchable/taggable/attachable declarations, a Linked Context target provider descriptor with provider-owned compact task labels/sorting, current-state product Help pages loaded from Markdown source paths, resume-safe task context, and a Notes-linked panel integration that consumes the Notes-owned helper instead of querying Notes storage; its developer handoff lives in `docs/tasks-module.md`. Notes contributes storage, Primary Context fields, Linked Context rows, Library-linking contracts, hierarchical Library collections, Markdown/revision/secure-note behavior, searchable/taggable/attachable declarations, a Linked Context target provider descriptor with safe Note target labels/sorting, current-state product Help pages loaded from Markdown source paths, conservative owner notifications, the protected Notes Library UI, the shared Add/Edit Linked Context picker adoption, framework modal-stack guardrails for Add/Edit utility dialogs, stacked Add/Edit Tags and Files dialogs, staged tag picker changes until Save Note, the reusable Linked Context panel helper, compact accessible Markdown toolbar controls including safe underline authoring, stable full-width toolbar placement, a wide-screen two-column Body/Preview editor body, narrow-screen stacked Preview behavior, and a scroll-safe Preview region that does not override framework modal footer ownership, plus safe Active Work resume-context candidates for future framework consumers; its developer handoff lives in `docs/notes-module.md`, the shared context terminology contract lives in `docs/workflow-context-contract.md`, the shared provider/shell contract lives in `docs/linked-context-picker-contract.md`, and import planning lives in `docs/notes-import-planning.md`. Lists contributes storage for lists, list items, explicit catalog suggestions, and linked records; workspace-aware Shopping/Procurement labels; service/API/UI workflows for item execution, reusable lists, bill-of-materials finalization, linked records, progress summaries, a Linked Context target provider descriptor with safe List target labels/sorting, a permission-filtered Task/Note/Project/Business Client picker consumer in the wide Create/Edit List modal, and resume-safe context; searchable/taggable/attachable declarations; current-state product Help pages loaded from Markdown source paths; and the protected Lists UI. Its developer handoff lives in `docs/lists-module.md`. These modules stay on framework services for permissions, tags, search sync, file attachments, audit, events, Help, and module lifecycle, and none of them writes directly to framework search, file storage, or Notes tables owned by another module.

As of 0.33.5.27.32, module and repository database code must not hardcode raw seam-backed SQLite dialect when a `db.dialect` seam owns the operation. Use the provider-neutral database facade from `src/core/database.js`, bind values through named params, keep identifiers/static SQL fragments allowlisted, and route conflict writes, case-insensitive comparison/order, boolean storage, timestamp math, FTS/search syntax, JSON access, returned rows/identity, physical identity, and PRAGMA/introspection through `db.dialect` seams or provider-owned framework services. The whole-tree dialect guardrail rejects raw application call-site dialect such as `INSERT OR IGNORE`, raw `ON CONFLICT`, `COLLATE NOCASE`, `julianday(...)`, FTS5 `MATCH`/`bm25(...)`, SQLite JSON functions/operators, PRAGMAs, raw lowercase `rowid`, and raw `RETURNING` outside provider-owned or sanctioned compatibility paths.

As of 0.33.5.28.2, that database contract is the starting point for every new first-party or future third-party module. Modules must not introduce module-local SQL literal escaping, module-owned database clients, raw provider dialect, or cross-module table reads to work around the contract. If a module needs persistence behavior that the facade or `db.dialect` seams cannot express, the seam is framework/database work first and module code consumes it afterward. Browser descriptors and view adapters remain data consumers only; canonical filtering, paging, permission pruning, and database access stay in server-side routes, services, and repositories.

Secure notes use application-managed envelope encryption in the first implementation. `LONGTAIL_SECURE_NOTES_MASTER_KEY` or `SECURE_NOTES_MASTER_KEY` provides the server-side master/key-encryption key from environment/config/secrets storage. `LONGTAIL_SECURE_NOTES_KEY_VERSION` identifies the current write key version and is stored on secure notes and secure revisions so a future rotation pass can distinguish encrypted records without changing the Notes model. Notes generates a random per-note data encryption key, encrypts the secure body with AES-256-GCM, wraps the per-note key with the configured server-side key, and stores encrypted payload, encrypted data key, algorithm metadata, key version, nonces, auth tags, payload version, and encrypted timestamp. The permission-gated secure-note health surface reports whether secure-note encryption is configured, plus algorithm, payload, and key-version metadata, without exposing key material. Secure-note body text is not stored in normal body, excerpt, plaintext index, metadata JSON, audit/event/search rows, notifications, or browser storage. Browser/API responses also strip the encryption envelope fields so encrypted payloads and wrapped keys remain storage-only server metadata. Secure revision history lists and restore responses are metadata-only; decrypted secure revision bodies are exposed only by the dedicated revision-read endpoint after secure revision checks. Titles remain plaintext metadata in 0.33.2.x and the UI warns users not to put secrets in titles. The browser labels secure notes, keeps previews body-closed, hides client-visible visibility while creating secure notes, locks security-mode changes on existing notes, and normalizes secure/decrypt failures to a safe locked-state message. This protects database-at-rest exposure but is not zero-knowledge because a configured app server can decrypt after session and permission checks. Secure-note access is owner-only plus explicit `notes.secure.*` admins; normal Notes, Library, collection, linked-record, tag, and file permissions do not grant secure body access. Secure notes cannot be `client_visible`, and framework-managed file attachments remain blocked until a deliberate secure-attachment model exists. Existing plaintext `security_mode = secure` placeholders are blocked from activation until manually recreated or explicitly migrated through reviewed tooling. Operators must back up the server-side secure-note key outside the database; losing or misconfiguring it makes secure-note bodies fail closed and may make encrypted secure-note content unrecoverable.

## Active Fields

These fields are currently accepted by the manifest validator:

- `id`: required stable kebab-case module identifier used in storage, settings, dependency declarations, and module checks.
- `name`: required database/module label.
- `displayName`: required UI label.
- `description`: required human-readable summary.
- `terminology`: optional display-only labels grouped by workspace type. Supported groups are `default`, `business`, `personal`, and `family`; supported fields include `label`, `singular`, `plural`, `shortLabel`, `navigationLabel`, `description`, `createButton`, and `emptyState`.
- `category`: required module grouping, such as `core-workflow` or `core-admin`.
- `version`: required module contract/version marker. The five bundled workflow manifests (Clients/Projects, Lists, Notes, Tasks, and Time Tracking) report the current application version through `src/core/version.js`; example, framework-support, or independently versioned modules may keep their own contract marker.
- `enabledByDefault`: required boolean controlling new workspace module status.
- `canDisable`: optional boolean that declares whether Workspace Settings may disable the module.
- `historicalReadAccess`: optional boolean that allows disabled data modules to keep read-only access to old records.
- `browserApiRoutes`: optional array of Express routers mounted under `/api` for signed-in browser use.
- `publicApiRoutes`: optional array of Express routers mounted before browser session authentication.
- `migrationsDir`: optional URL/string module migration folder. Core migrations still run first.
- `protectedViewsDir`: optional URL/string ownership hint for authenticated module pages.
- `publicViewsDir`: optional URL/string ownership hint for public module pages.
- `browserAssetsDir`: optional URL/string ownership hint for module browser assets.
- `protectedViews`: optional authenticated module page descriptors.
- `publicViews`: optional public module page descriptors, reserved for later public module pages.
- `browserAssets`: optional browser asset descriptors for module-specific scripts or styles.
- `navigation`: optional app-shell link descriptors.
- `dashboard`: optional Dashboard panel descriptors.
- `reporting`: optional validated, data-only report contribution descriptors.
- `workbench`: optional Workbench card contribution descriptors.
- `settings`: optional module settings descriptors.
- `permissions`: optional permission descriptors with user-facing labels and descriptions.
- `requiredPermissions`: optional permission IDs expected by the module.
- `defaultRolePermissions`: optional role-to-permission defaults to sync into `role_permissions`.
- `resourceDefinitions`: optional module resource keys, supported operations, and required permissions used by the User Admin permission catalog.
- `publicApiEndpoints`: optional public API documentation/discovery descriptors; each endpoint declares a known `publicDemoCapability`.
- `apiScopes`: optional API key scope descriptors provided by the module; each scope declares a known `publicDemoCapability`.
- `auditRecordTypes`: optional audit record type descriptors accepted by the audit service.
- `timerSources`: optional timer-capable source declarations.
- `workItemSources`: optional actionable Workbench item source declarations.
- `linkedContextProviders`: optional module-owned Linked Context target provider descriptors.
- `eventTypes`: optional module event descriptors emitted through the internal event bus.
- `eventSummaries`: optional activity-safe and notification-safe event summary descriptors.
- `hooks`: optional hook object with lifecycle functions and `events` subscriptions.
- `frameworkDependencies`: optional framework service dependency IDs.
- `moduleDependencies`: optional registered module IDs that must exist.
- `workspaceCapabilityRequirements`: optional workspace capability keys that make the module relevant.
- `notificationEvents`: optional module-declared notification event descriptors.
- `notificationTemplates`: optional module-declared notification template descriptors.
- `notificationFollowTargets`: optional module-declared target types that users may follow for per-record notification overrides.
- `taggableTypes`: optional module-declared taggable record type descriptors.
- `searchableTypes`: optional module-declared searchable record type descriptors.
- `attachableTypes`: optional module-declared file attachment target descriptors.
- `protectedContentConsumers`: optional module-declared protected-record consumer policies.
- `help`: optional module-declared Help Center contribution block with `sections` and `articles`.
- `seedHooks`: optional reserved startup hook array.
- `repairHooks`: optional reserved startup repair hook array.

## Reserved Fields

There are no search-reserved manifest fields as of 0.32.9.1. `searchableTypes` is an active validated contract. Help uses the active validated `help` contribution block. Future extension data should be promoted deliberately with validator coverage instead of being accepted as arbitrary metadata.

Notifications are framework-owned. Modules declare notification events, templates, and followable target types through the manifest, but individual modules should not create duplicate notification UI.

Protected-content consumer descriptors require `id`, `moduleId`, `recordType`, `surface`, `behavior`, and `assertion`. `behavior` is either `authorize` or `exclude`; IDs must be unique within the module. The manifest is data-only: the assertion names a module-owned policy implementation, and each actual consumer must call that implementation rather than reconstructing protection from a storage flag. As of 0.33.29.3, Notes uses `notes.effective-security`; unknown consumer IDs fail closed, and Support View, Files, notifications, Search, resume/Workbench, public API, exports, and generic provider catalogs declare exclusion. A future consumer cannot gain secure-content access merely by registering a descriptor.

Protected browser GET/HEAD routes also participate in the framework-owned Support View route catalog. Each route has one stable ID and is classified read-safe or sensitive; the release-gate source audit fails when a protected read is added without a declaration. All other methods are centrally mutation-denied before module dispatch, and module manifests or handlers cannot weaken that rule. Route declarations never authorize content by themselves: normal module permissions and protected-content assertions still run under the effective target identity.

Module browser code must not infer Support View authorization from disabled controls or rebuild the shared banner. The app shell owns the persistent actor/target/workspace/expiry state and generic write-control suppression; modules must continue to shape secret-bearing responses before render, keep meaningful read controls identifiable, and rely on their normal server permissions plus the central method gate. A module that adds a protected read must classify it in the framework catalog and prove any secret/content exclusion at its service boundary.

## Contribution Shapes

Navigation items require `label` and `href`; they may include `parent`, `requiredPermissions`, and display-only `terminology`.

The authenticated app shell reads module navigation through `/api/app-shell/bootstrap`. The backend combines framework-owned navigation such as Dashboard, Workbench, workspace settings, user settings, workspace switching, API keys, and audit links with enabled module navigation from the registry. The browser app shell renders the returned tree directly and keeps the static browser nav only as a fallback while bootstrap data is loading or unavailable.

Reporting contributions require `id`, `moduleId`, `label`, `description`, `category`, `renderer`, `runner`, explicit `requiredPermissions`, `requiredWorkspaceCapabilities`, and `requiresEnabledModules` arrays, a `filters` array, and one or more `browserAssetIds`; `sortOrder` is optional. The three requirement arrays and `filters` may be empty when a report has no such requirements, but they remain explicit so contribution intent is reviewable. IDs, runner IDs, and renderer IDs are stable data identifiers; executable runners and browser renderers register outside the manifest.

Supported report filter descriptors require `id`, `label`, a validated `type`, and safe `queryKeys`: `billing-period`, `scope`, `project-multi-select`, `tag`, and `boolean` use exactly one query key, while `custom-date-range` uses exactly two. Query keys must be unique within a report. Filters may declare a type-matched `defaultValue` (string for scalar filters, string list for project/tag filters, boolean for boolean filters), `required`, and a data-only `visibleWhen` dependency with `filterId` plus string `equals`. Billing-period defaults are limited to `current`, `last`, or `custom`. References must resolve inside the same report, and unknown filter or report fields are rejected.

`modulesService.listReportingReports(workspaceId, session)` uses the shared contribution pipeline: the owning module must be enabled, every `requiresEnabledModules` entry must be enabled, a relevant workspace capability must be available, and every required permission must pass before the contribution is returned. The function sorts metadata only and never resolves or runs the `runner`. A module's `historicalReadAccess` affects its historical record pages, not executable report/catalog availability.

Settings contributions require `id`, `label`, `type`, and `placement`. Supported types are `boolean`, `toggle`, `text`, `number`, `select`, `multi-select`, `radio`, and read-only `info`. The fixed placement set is `workspace`, `user`, `module`, and `new-workspace`. A contribution may also declare `target` (default `module`), `ownerOnly`, `readOnly`, `description`, `placeholder`, `inputmode`, read-only/disabled reason text, `options`, `min`, `max`, `step`, a type-matched `default`, `required`, `moduleStatus`, display-only terminology, and the standard `requiredPermissions`, `requiredWorkspaceCapabilities`, `requiresEnabledModules` / `requiredModules` eligibility axes. Select, multi-select, and radio settings require registered options; defaults must match the declared type, options, and numeric range.

Settings manifests are data-only. Optional `handler` and `onChangeEffect` values are stable string IDs; executable persistence handlers and effects register outside the manifest. Unknown fields, functions, duplicate setting IDs or options, unknown permission/module references, and invalid metadata fail startup validation. A module contribution cannot set `target: "framework"`, set `protected: true`, or reuse a framework-registered setting ID. Only framework registration may create a protected or framework-target definition.

`modulesService.listSettingsContributions(workspaceId, session)` is the permission-safe metadata catalog. It delegates to the shared contribution pipeline, so the owning module and every declared module dependency must be enabled, workspace capabilities must match, and all required permissions must pass; display terminology is resolved for the workspace. The result defaults omitted targets to `module` and sorts metadata, but does not read setting values, resolve handlers, or run effects. Value reads and writes remain separate through `settingsService`.

As of 0.33.21.5, Notes registers `notes-settings.html` as an eligible protected module destination and contributes a read-only `catalogManagement` information descriptor at the `module` attachment point. Its catalog manager mounts in the shared module host's auxiliary region and builds its table, bulk toolbar, fields, actions, and modal through `LongtailForge.view`. The framework owns page/navigation eligibility and shared anatomy; Notes owns `GET /api/notes/settings/catalogs`, `POST /api/notes/settings/catalogs/bulk`, canonical collection create/edit calls, permission checks, hierarchy/lifecycle meaning, audit, search synchronization, and partial-failure behavior. A catalog here is the existing `note_library_collections` Collection hierarchy, not a scalar setting or a new storage model. Both `notes.manage_settings` and `notes.manage_library` are required, and Secure Notes runtime keys are never included.

As of 0.33.12.3, `GET /api/reporting/catalog` exposes the framework-owned permission-safe catalog. Each entry uses the stable namespaced key `<moduleId>:<reportId>` and returns report metadata, renderer ID, validated filters, defaults keyed by filter ID, report requirements, and only the referenced same-module renderer assets that also pass enabled-module, capability, permission, and `framework:reporting` host filtering. The browser catalog does not expose or resolve the server runner ID, and listing never executes report code. If a required renderer asset is not allowed for the current session, the report is omitted instead of returning an unusable or partially authorized contribution.

`GET /api/reporting/reports/:reportKey/run` re-resolves the allowed catalog entry on every request, validates known query keys plus scalar/list/boolean/date basics from its filter metadata, and only then resolves the manifest's runner ID through `src/core/reporting/report-runner-registry.js`. Module startup code registers executable runners by stable ID outside the manifest. Duplicate registration fails unless an explicit replacement is requested; missing registration returns a safe temporary-unavailable response. Successful execution returns the framework envelope `{ status: "ready", reportKey, renderer, result }`. Unknown/ineligible reports, invalid filters, missing runners, record-access failures, and unexpected runner errors return framework-owned status/error envelopes without runner IDs, hidden record labels, database messages, stack details, or other implementation data. The module-owned runner still owns domain validation, data reads, calculations, result shape, and record-level permission enforcement.

As of 0.33.12.4, Time Tracking registers `time-tracking.project-time-billing` during module startup. `src/modules/time-tracking/time-tracking-billing.service.js` is the single permission-checked calculation layer for the runner and retained compatibility reads; it owns time-entry aggregation, report periods, project hierarchy display rows, billing metadata application, and the JSON-safe result. The existing `/api/reporting/bootstrap` and `/api/reporting/project-summary` paths remain temporarily available for the pre-conversion page, but their handlers now live in the Time Tracking module. The framework Reporting routes and service contain only the catalog and namespaced generic execution path and must not import or name first-party module services, repositories, reports, or IDs.

The Project Time & Billing contribution explicitly requires `time-tracking` and `client-projects`. Time Tracking reads permission-shaped client/project hierarchy and billing metadata through the Clients/Projects-owned `clientsService.readClientProjects(session)` service contract; it does not duplicate Clients/Projects queries or hierarchy permission logic. Disabled Time Tracking removes the catalog entry before dispatch and also blocks the retained compatibility execution path despite historical record readability.

As of 0.33.12.5, the provider/calculation boundary preserves owner-specific billing rules across recursive hierarchies. Clients/Projects returns descendant client IDs as the `childScopeIds` array and keeps each project attached to its owning client metadata. Time Tracking derives each project’s effective rate, period, and rounding from that owner context, summarizes only direct entries for that project, then recursively adds already-rounded/priced child branches. Report footers sum root rows only; nested `childRows` are expandable display rows and are never added a second time. Parent-client summaries include descendant-client project branches, while workspace/dashboard totals reduce only top-level client scope summaries because those parents already contain their readable descendants.

Reporting remains a framework-owned host rather than a normal module or a module-owned `viewSurfaces` page. The chosen host path is a narrow framework adapter built from `LongtailForge.view` primitives: framework code owns the page shell/header, report selector, common filter host, status/loading/error/empty states, results/overflow host, renderer dispatch, and future report-action placement. Modules own definitions, runner/renderer registration, data access, domain math, result shapes, readable labels, record-level permission checks, and result rendering. The contribution contract did not pre-implement the host, and the shipped host does not pre-implement the 0.33.14 editable-field factory.

As of 0.33.12.6, the Reporting host is live. `views/protected/reporting.html` contains only the framework mount plus shared navigation/view/formatting/tag assets and `public/js/reporting.js`; it contains no report controls, result table, or module renderer asset. The browser host reads `/api/reporting/catalog`, chooses the requested eligible report or the first available entry, constructs common controls from the selected contribution's filter metadata, applies `visibleWhen`, preserves the selected report and active filters in the query string, loads only that entry's returned renderer assets, and executes only `/api/reporting/reports/:reportKey/run`. Empty catalogs, missing renderer registrations, safe execution failures, no-result responses, and renderer failures use framework-owned status/empty/error anatomy.

Module browser assets register through `LongtailForge.reporting.registerRenderer(rendererId, adapter)`. The adapter must provide `render(result, context)` and may provide `initializeFilters(context)`, `synchronizeFilters(context, changedFilterId)`, and `validateFilters(context)`. Those hooks may hydrate module-owned option data and dependent controls, but the framework remains responsible for filter control placement, change dispatch, allowed-asset loading, execution, and result-host placement. Time Tracking's adapter uses its module-owned bootstrap/tag reads for readable scope/project/tag choices, registers `time-project-billing-table`, renders nested runner-provided rows with shared table/action primitives, and displays the runner's root-only `totals` directly. The converted host never calls `/api/reporting/project-summary`; that route remains compatibility-only during the branch.

As of 0.33.12.7, `reporting.view` is defined by the framework permission catalog, not by any report-contributing module. The framework catalog also owns the `reporting` resource definition and preserves the existing Super Admin, Workspace Admin, Client Admin, Project Admin, Client User, and Project User defaults. A module report and its renderer asset may require `reporting.view`, but a disable-able module must not define or grant that permission. The app shell builds Reporting child navigation from `modulesService.listReportingReports(workspaceId, session)`, using stable `<moduleId>:<reportId>` deep links; when no eligible reports remain, neither the Reporting menu nor its quick action is delivered, and the direct host renders a permission-neutral empty state without naming unavailable contributions.

The 0.33.12.7 strict guardrail closes the Reporting conversion. The protected HTML stays a minimal framework mount, framework anatomy stays on `LongtailForge.view`, direct DOM creation in the host is limited to allowed renderer asset loading, and framework Reporting HTML/browser/service code must not name or import a first-party module, report, runner, renderer path, service, repository, or result shape. Those concerns stay behind the validated contribution plus registered module runner/renderer seams.

Protected view descriptors require `id`, `path`, `moduleId`, and `file`; they may include `requiredPermissions`, `requiredWorkspaceCapabilities`, and `allowDisabledRead`. The static view service serves protected module pages only when a registered view matches the requested path. Unknown protected HTML files are not served merely because they exist under `views/protected`.

Framework-owned protected views are registered by the framework rather than by optional workflow modules. Dashboard, Workbench, Workspace Settings, User Settings, Calendar Settings, API Keys, Audit Log, and Reporting remain framework-owned. Calendar Settings is permission-gated by `workspace_settings.manage` and remains available for safe credential listing/revocation even when Tasks is disabled; Workbench is specifically not owned by Tasks, Time Tracking, Notes, Support Tickets, or any other workflow module.

When a protected module view belongs to a disabled module, the view service returns a disabled-module response unless the descriptor sets `allowDisabledRead: true` and the module allows historical reads. Permission checks are server-side; `requiredPermissions` on the view descriptor is treated as the page-level gate before the module's APIs enforce record-level access.

A module Settings view may use `allowDisabledRead: true` as a lifecycle recovery route. It must retain the framework Settings host and permission gate, render an explicit disabled state instead of active module controls, and point administrators to Workspace Settings. Successful lifecycle saves refresh the framework app-shell bootstrap so module navigation and quick actions update immediately while the Workspace recovery route remains available.

Browser asset descriptors require `id`, `moduleId`, `path`, and `type` (`script` or `style`); they may include `views`, `requiredPermissions`, and `requiredWorkspaceCapabilities`. Declare a clean local asset path without a manual cache query. The module service normalizes registered JavaScript/CSS asset paths through the application-derived asset-version helper before exposing them. Common app-shell assets such as navigation, theme initialization, workspace switching, global search placeholders, notification placeholders, and shared CSS remain framework-owned. Module-specific page scripts/styles should be declared by the owning module and loaded only on the pages that need them.

Reporting renderer assets use the explicit `framework:reporting` value in `views`. Every asset ID referenced by a `reporting` contribution must resolve to a browser asset declared by the same module and targeted at that host. Reports may not provide arbitrary URLs, inline functions, or another module's asset ID.

As of 0.33.5.21.9.4, user theme preference handling is framework-owned. `theme_mode` accepts `light`, `auto`, and `dark`, while `theme_auto_source` stores the active auto resolver. The only shipped auto source is `system`, which resolves through `prefers-color-scheme` before first paint via the theme cookies/localStorage keys and re-resolves on operating-system color-scheme changes in the authenticated shell. Sunrise/sunset theme automation is deferred until a future location contract exists; modules should not infer daylight from timezone or add module-specific theme resolution.

Framework-owned UI surface contracts live in `docs/ui-surface-contract.md`. Shared CSS classes and browser helpers define modal group structure, modal footer/action placement, overlay host behavior, drawer/slideout shells, main-screen internal panels, dense row/list action placement, chips, dividers, and focus treatment. Modules should consume these classes and helpers rather than inventing one-off modal boxes, footer layouts, overlay behavior, or record-row action shells. Modules remain responsible for their own fields, picker content, validation, API calls, permission checks, and record-specific business semantics.

Framework-owned view-building primitives live in `docs/view-building-contract.md`. `LongtailForge.view` is implemented in `public/js/shared/view-builder.js` as the current imperative helper namespace. The helper layer owns common DOM anatomy such as page headers, status messages, empty states, filter panels, collapsible selector/index panels, split list/detail workspaces, list shells, data tables with overflow wrappers, detail headers, metadata/badge rows, action strips, summary panels, modal shells/forms/footers, modal-stack opening and closing guardrails, field grids, inline item/action rows, and the shared Linked Context picker shell. It must stay smaller than a frontend framework: modules keep data loading, state, validation, API calls, save payloads, route permissions, labels, and workflow behavior.

Converted module surfaces should use `LongtailForge.view` helpers for framework-owned anatomy when a matching helper exists. Converted surfaces should not directly create dialog shells, modal footer structures, or dense/detail action rows that the helper already owns, and should not introduce hard-coded light backgrounds outside shared theme tokens. Non-converted surfaces may remain hand-built until an explicit conversion slice names them; the framework view-building contract is adopted surface by surface, not by silently rewriting every protected page.

`viewSurfaces` is the declarative manifest field for framework-rendered protected views. As of 0.33.5.16.12, the backend validates descriptor shape and references, resolves descriptor labels through the existing workspace terminology resolver, exposes active descriptors through the existing app-shell bootstrap payload, and the browser renderer at `public/js/shared/view-renderer.js` can fetch descriptor data sources through `LongtailForge.api.getJson`, render descriptor select filters, expose stable generic field hooks, preserve basic field attributes, dispatch declarative actions through route metadata or registered behavior handlers, and expose descriptor-backed wrappers for action strips, action menus, field grids, tables, modal forms, inline action rows, and linked-record panels. Lists now uses a descriptor for its protected workspace read shell, item form fields, item table/action placement, list-level workflow actions, linked-record picker/row placement, and create/edit modal shell. Notes now uses `notes.workspace` for the protected slide-out action surface. Tasks now declares `tasks.workspace` for the protected page shell/read path; its task list shell, bulk toolbar shell, row lifecycle action strip, row workflow action menu, add/edit task modal shell, helper-built modal sections, and detail metadata use framework helpers, while task rows, bulk semantics, payloads, validation, assignment, scheduling, recurrence, checklist, timer, tags, files, and notes behavior remain module-owned. Files exposes the framework-owned `files.browse` descriptor through the framework descriptor registry. Strict declarative guardrails currently enforce `client-projects.clients`, `client-projects.projects`, `files.browse`, `lists.workspace`, `notes.workspace`, and `tasks.workspace`; Tags and Developer Example descriptors remain inventoried/reported. As of 0.33.5.18.14.5, Clients/Projects also declares strict descriptors, `client-projects.clients` and `client-projects.projects`, bound to `/api/clients?include_depth=true` and `/api/projects?include_depth=true`; those read pages render headers, slide-out filters, status/empty states, hierarchy display, secondary tag rows, table wrappers, row-selection checkbox anatomy, page actions, icon-only repeated edit actions, related read rows, and bulk-toolbar shell regions through descriptor/shared renderer paths while descriptor/query Add/Edit dispatch routes through the shared module-action registry to the Clients/Projects-owned dialog API. Clients/Projects still owns row shaping, service-owned Projects hierarchy ordering, related-row meaning, selected IDs, bulk action meaning, payloads, route calls, and workspace gating. `/api/client-projects` remains the option/dialog workflow route. A descriptor requires `id`, `moduleId`, `viewId`, `layout`, and `dataSource.fieldBindings`; bundled list descriptors also declare the exact array envelope as `dataSource.recordsKey`. The checked `LongtailForge.viewResponseRecords` adapter prefers that key and contains compatibility selection for older or external descriptors, so renderers and module adapters must not guess response keys. Supported layouts are `single-column`, `stacked`, `sidebar-detail`, `slide-out-sidebar`, and `table-page`; `split-list-detail` is retired and compatibility-only. Descriptors may declare `pageHeader`, `filters`, `filterPlacement`, `indexPanel`, `sidebarPanels`, `table`, `detail`, `modals`, and `actions` using framework-owned anatomy names. As of 0.33.5.18.13.1, filters may hydrate module-owned options through `optionsSource`, index/table descriptors may carry display-only depth/parent/path metadata, and table columns may use the framework-known `hierarchy-label` or `chip-list` display formatters. As of 0.33.5.18.13.3, `table.rowActions` may render through a framework-owned action column while registered behavior handlers keep row action meaning module-owned. As of 0.33.5.18.14.3, `table.selection` may render framework-owned row-selection checkboxes while the module owns selected-ID state and bulk workflow behavior. As of 0.33.5.18.14.5, `filterPlacement: "slide-out-sidebar"` may move table-page filters into the shared drawer, `table.secondaryRows` may render supporting row metadata such as tag chips, and actions may declare `icon`, `iconOnly`, and `title` for dense repeated placements. Manifest descriptor `moduleId` values must reference a known module, `viewId` values must bind to a protected view for that module, surface IDs must be unique across loaded modules, action `requiredPermissions` must reference known core or module permissions, action roles are limited to `primary`, `secondary`, `destructive`, and `utility`, and descriptor routes must be local app paths with supported HTTP methods.

As of 0.33.32.22, the checked `ViewSurfaceDescriptor` declaration family, manifest validator, and `LongtailForge.viewSurfaceDescriptor.normalize(unknown)` browser projection are one descriptor-field boundary. The projection runs before the renderer reads delivered JSON, retains the service-supplied `viewPath` and page-local nullable data-source opt-out, and rejects unknown or wrong-typed supported fields instead of silently falling back. Summary-panel `messageField`/`items` metadata and string-or-object action confirmation copy are explicit shared contracts. Open detail and empty-state payloads remain extension points only where the validator already allows open objects; modules must not add ad hoc top-level or nested descriptor keys.

As of 0.33.31.2, every registered declarative action, public API endpoint, and API scope declares a known `publicDemoCapability`. While the exact public-demo profile is active, disabled or undeclared contributions are removed from presentation catalogs and rejected at their server boundary; all `/api/v1` routes are classified as `api_keys` and unavailable. This filtering is presentation only. Route and service enforcement remains authoritative, and normal installations retain the existing catalog and behavior.

As of 0.33.21.2.1, `table.selection.headerLabel` and `table.rowActionsHeaderLabel` may override their visible utility-column headings, including with an explicit empty string. This presentation option does not remove the accessible names on row checkboxes or actions.

As of 0.33.21.2.2, the Add Client dialog uses framework modal-form fields and enters the shared modal stack, while Clients/Projects continues to own field meaning, tags, routes, payloads, and completion details. Add Project invokes the registered `clients.add` module action as a stacked child workflow and consumes only its completed record ID to refresh and select the new Client. Workspace-level Project options use the readable workspace label and default non-billable in both the browser form and service normalization.

As of 0.33.21.2.3, the Clients/Projects-owned Edit Project body requests a modal-only layout inside the framework `size: "wide"` shell. The modal layout removes the embedded related-row disclosure/card, orders Status, Client, and Parent Project as full-width fields, renders Project Tags full-width without another bordered fieldset, and places Task Reminder Defaults inside the Task module group under Project Defaults. Embedded related-Project editors keep their existing disclosure layout, and Clients/Projects retains save/archive, reparent confirmation, billing, defaults, tags, validation, payload, route, and completion ownership.

As of 0.33.21.11.1, Client-aware Project contributions are Business-only at the Clients/Projects boundary. Before `client-projects.projects` reaches the shared renderer, the module adapter removes the Client filter, Client column, Client index subtitle, and Client data bindings for Personal and Family workspaces. Add/Edit Project returns no Client field outside Business rather than appending hidden modal anatomy. The service ignores injected Client filters on project-only list reads, omits Client-backed rows, strips Client context from detail reads, and rejects nonblank Client assignment through both Client-scoped routes and accepted payload spellings. Business descriptor, modal, read, and write behavior remains unchanged.

As of 0.33.21.11.2, the dedicated Edit Project modal renders Rounding and Task Reminder Defaults inside the module-owned Project Defaults disclosure, with Task Reminder Defaults immediately below the Task module settings. The former Project Rounding disclosure is not rendered when it would be empty, while the existing rounding/reminder readers, inheritance controls, validation, and save payloads remain unchanged.

As of 0.33.5.18.14.1, Clients/Projects read pages use registered behavior handlers for descriptor Add/Edit actions, and those behavior handlers plus the query-param openers dispatch through `LongtailForge.moduleActions.open(...)`. The shared module-action registry owns the first-party action metadata and host-context envelope, while `public/js/clients-projects.js` publishes `LongtailForge.clientProjectDialog` and still owns dialog openers, option hydration, safe labels, billing display values, tag display values, Project Client labels, save payloads, refresh behavior, permission implications, and hierarchy rules. The adapter no longer duplicates first-party action registration or the old page-level Add Client compatibility form.

As of 0.33.5.18.14.2, Clients/Projects related read regions use framework-owned `LongtailForge.view` list, data-table, collapsible-region, and dense action-strip helpers. Client detail reads show related Projects through those helpers, and Project editor reads show Client plus Parent Project context rows through the same shared anatomy. The adapter still owns related-row shaping, readable labels, billing and task-default summaries, tag chip content, allowed row actions, route calls, save/archive/reparent behavior, and permission semantics.

As of 0.33.5.18.14.3, Clients/Projects bulk read controls use descriptor table selection plus `LongtailForge.view.createBulkActionToolbar()` for framework-owned checkbox and toolbar chrome. The adapter still owns selected Client/Project IDs, allowed status/billing/Client reassignment controls, Project Client reassignment gating for Business workspaces only, granular `/api/clients/:id` and `/api/projects/:id` update calls, confirmations, partial-failure messaging, descriptor refreshes, and permission/audit/search/event implications.

As of 0.33.5.18.14.4, Projects canonical read ordering is owned by the Clients/Projects services: workspace-level Projects sort first, then readable Client-backed Project groups follow Client hierarchy order, and Project parent/child hierarchy is ordered inside each group. The renderer must not own Project grouping, parent validation, cycle prevention, same-scope checks, archived-parent checks, Business-only Client behavior, workspace Project behavior, or drag/drop hierarchy editing. Descriptor actions continue to open the existing Clients/Projects editors, and those editors submit existing validated payloads through Clients/Projects services.

As of 0.33.5.18.14.5, Clients/Projects strict page guardrails are active. The framework owns the converted page/read anatomy, slide-out filter surface, secondary tag-row anatomy, icon-only repeated table edit controls, descriptor row-selection checkboxes, related read shells, and bulk toolbar shell. The module owns option hydration, tag assignment, selected IDs, bulk choices, route calls, payloads, confirmations, partial failures, dialog bodies, billing/default editors, parent selectors, hierarchy validation, readable labels, query-param openers, refresh hooks, and workspace gating. This did not add schema, route payload, permission, or workflow changes.

As of 0.33.5.18.15, the view-conversion branch closeout records the final strict/deferred surface boundary. Strict declarative guardrails currently enforce `lists.workspace`, `notes.workspace`, `tasks.workspace`, `files.browse`, `client-projects.clients`, and `client-projects.projects`. Tags management and Developer Example descriptors remain reported descriptor proofs. Admin/Settings, Reporting, Dashboard, Workbench, pagination/server-side paging, Inspector behavior, and non-view workflow changes are deferred to later roadmap lines. This closeout does not add schema, write payload, permission, public API, route, or workflow changes; it updates the docs, Decisions, changelog, version metadata, archive handoff, and closeout regression coverage for the completed 0.33.5.18 branch.

As of 0.33.5.18.11.1, framework-owned protected pages may also expose descriptors through a framework descriptor registry rather than a module manifest. Files uses this path for `files.browse` with `moduleId: "framework"` because the Files framework owns storage, access, download, lifecycle, scan/quarantine, and shared attachment behavior while modules own the business meaning and placement of file attachments. The modules service merges framework-owned descriptors into the same app-shell `viewSurfaces` payload and applies the protected-view `files.view` gate before delivery. As of 0.33.5.18.11.2, the Files browse descriptor uses the shared `slide-out-sidebar` layout with a Files-owned filter behavior in the drawer and a Files-owned results behavior in the main panel. Normal Client and Project filters use readable select controls backed by the existing client/project provider where available; raw module, target, and fallback project IDs stay behind an explicit advanced troubleshooting disclosure. Client filters remain Business-workspace-only and are hidden, disabled, and omitted from route queries outside Business workspaces. As of 0.33.5.18.11.3, the Files results behavior mounts the browse rows through the shared list shell/data table helpers, while Files still owns `/api/files/attachments` reads, row shaping, readable labels, scan/status fallback values, download/delete/restore availability, confirmations, route calls, and refresh behavior. Normal browse rows use safe file-type badges with extension text, truncated filename/target/Client/Project labels with one custom body-level hover/focus reveal, and accessible icon-only repeated row controls without exposing storage paths or raw protected IDs. As of 0.33.5.18.11.5, Files browse is intentionally compact again: the 0.33.5.18.11.4 inline summary/detail/preview/metadata stack and selected-row state are not part of the normal browse page. The results region keeps status plus the table/list only. As of 0.33.5.18.11.6, Files exposes `PATCH /api/files/attachments/:fileAttachmentId/context` for attachment-scoped context updates. The route updates `file_attachments` only, derives Client/Project from the registered target, checks old-context remove permission and new-context attach permission, rejects conflicting selector payloads, audits previous/next context, and emits safe `file.attachment.context_updated` lifecycle metadata without exposing storage keys, storage paths, hashes, scanner internals, or protected filesystem data. As of 0.33.5.18.11.7, Files exposes `GET /api/files/attachable-targets` as the read-only provider for future File Context modal choices; it returns permission-shaped registered targets with readable labels and Business-only Client option payloads while keeping protected IDs internal to option values. As of 0.33.5.18.11.8, Files exposes `LongtailForge.filesDialog.openFileEditor()` as the canonical File Context modal opener. The modal uses shared view modal/form/footer helpers, shows read-only file metadata, loads target options from the provider, and limits controls to context selectors. As of 0.33.5.18.11.9, persisted browse rows open that modal on click or focused-row Enter while row Download/Delete/Restore controls stay isolated, and the modal saves attachment context through `PATCH /api/files/attachments/:fileAttachmentId/context` with only target identity plus optional Business Client/Project selector hints. Files remains the owner of route calls, refresh behavior, inline error status, and focus return, while the service remains authoritative for permission checks, target validation, and derived Client/Project context. As of the 0.33.5.18.11.12 follow-up, File Context orders controls as Client, Project, then Target; Client and Project controls are populated from the shared `/api/client-projects` option source with hierarchy ordering, remain stable while targets are filtered, and target options are loaded across active registered attachable types for the selected Client/Project context, so readable Notes and Tasks may both appear instead of being restricted to the current attachment module/type. Target option labels omit Client/Project context already shown by selected Context controls. The File Context modal edits the selected `file_attachments` row and may expose the same standalone Preview action as the Files list for previewable attachments, but it still does not add rename, replacement, storage, scan/quarantine, hard-delete, purge, or download-only metadata editing. As of 0.33.5.18.11.10, Files exposes `GET /api/files/attachments/:fileAttachmentId/preview` as a preview availability route. The route resolves the selected attachment target, requires target read access plus context-scoped `files.download`, classifies availability as `previewable`, `download_only`, `too_large_for_preview`, `unavailable`, or `unauthorized`, classifies kind as `image`, `text`, `markdown`, or `unsupported`, and returns readable filename/file-type data without content bytes, storage keys, storage paths, hashes, scanner internals, or preview audit/lifecycle events. As of 0.33.5.18.11.11, previewable descriptors include `contentAvailable` plus route-backed `contentUrl` values for `GET /api/files/attachments/:fileAttachmentId/preview/content`; image previews stream through authenticated Files routes, text and Markdown previews return capped JSON content, Markdown rendering uses the shared server Markdown service, raw HTML stays disabled, unsafe links/images remain neutralized, and no storage keys, protected paths, hashes, scanner internals, or raw filesystem URLs are exposed. As of 0.33.5.18.11.12, the Files browse row Preview action opens a dedicated shared-helper Files Preview modal through `LongtailForge.filesDialog.openFilePreview()`, while row click still opens File Context and Download/Delete/Restore stay distinct row controls. The Preview modal reads the descriptor/content routes, renders images/text/Markdown in constrained scroll-safe regions, includes Download when available, and handles loading, download-only, too-large, unavailable, and permission states without adding an inline preview pane or Inspector behavior. Preview descriptor/content/modal reads do not emit preview audit/lifecycle events in this slice; add `file.previewed` later only if tracking becomes useful. As of the 0.33.5.18.11.13 closeout, the Files browse/edit/preview branch is closed with a compact workspace file recovery surface: filter sidebar owns browse filtering, the main panel owns the listing only, row click opens the File Context edit modal, Preview opens the Files Preview modal, File Context metadata is read-only, context editing and Preview are attachment-scoped and route-backed, and Inspector integration remains deferred. As of 0.33.5.18.12.1, the shared Files attachment helper renders the upload/dropzone shell, accepted-file hint, upload action, progress/status, and per-file result placement through shared view helpers when they are available, while streamed multipart form construction, accepted categories, target context, visibility, `/api/files/upload/batch`, legacy `/api/files/batch` compatibility, partial failures, refresh, and host callbacks remain Files-owned. As of 0.33.5.18.12.2, the shared Files attachment helper wraps its reusable attachment panel, panel status, attachment list, empty/loading/error/save-first states, upload-result rows, and dense action shell through shared view helpers when they are available. Attachment reads, upload/list/download/remove/delete/restore logic, confirmations, refresh, events, save-first behavior, deleted/unavailable/quarantined recovery-state copy, and host callbacks remain Files-owned; the helper still must not reimplement the already-shipped File Context or Preview routes/modals. As of 0.33.5.18.12.3, Files row and attachment-panel actions use shared Files action IDs for Preview, Download, Report, Quarantine, Remove Attachment, Delete, and Restore while browser helpers keep the shipped Files routes, confirmations, permission-shaped visibility, refresh behavior, unsupported download-only state, and service-owned permission/lifecycle authority. As of 0.33.5.18.12.4, Files row and attachment-panel visual states use wrapping availability/review chips, attachment count/empty-state parity, broad recovery/review language, and responsive action rows while preserving Files-owned routes, permissions, File Context, Preview, unsupported download-only behavior, and compact browse boundaries. As of 0.33.5.18.12.5, the Files strict guardrail inventory maps future framework-owned anatomy and allowed Files-owned escape hatches. As of 0.33.5.18.12.6, Files strict declarative guardrail enforcement keeps `files.browse` strict, fails reintroduced hand-built protected-page/filter/table/panel/upload/action anatomy and inline detail/preview/metadata/Inspector behavior, and preserves documented Files-owned route, file-read, upload-payload, availability, permission, recovery, File Context, and Preview escape hatches.

As of 0.33.5.18.12.7, the Files browse/edit/preview/upload/action/strict-guardrail conversion branch is closed at the compact listing-first boundary. The main Files page remains a recovery/audit listing with filters in the slide-out sidebar, listing-only main content, route-backed File Context and Preview modals, shared attachment/upload/action shells, and strict `files.browse` guardrails. Future Files work must explicitly reopen scope before adding inline detail, inline preview, inline metadata, Inspector behavior, rename/replacement, storage moves, hard purge, permanent delete, or raw storage controls.

The current File Context lifecycle exception is `Mark Reviewed`: for in-review files that already passed scan or require no scan, the modal may call the existing Files restore route after service-owned permission checks. It must not expose raw scan/quarantine metadata controls, storage controls, rename, replacement, hard-delete, purge, or inline preview/metadata bodies.

As of 0.33.5.18.6.11, Notes is the first `slide-out-sidebar` adopter. The framework owns the action-surface shell, screen-left funnel trigger, off-canvas drawer, backdrop, focus/ARIA state, scroll locking, footer-aware trigger placement, panel shell, and central primary/detail anatomy. Notes owns filters, Library and collection behavior, Notes List data, sort and pagination, selection behavior, selected-note detail rendering, Primary Context, Linked Context, tags, files, and Markdown workflows. Future action/workflow conversions should reuse this pattern when side navigation is needed, rather than reviving the retired `split-list-detail` center split or treating persistent `sidebar-detail` columns as the default direction.

As of 0.33.21.12, the Notes adapter scopes its own visibility descriptors rather than asking the framework renderer to know Notes semantics. Personal workspaces remove visibility from filter, editor, bulk, badge, and metadata contributions and omit the field from save payloads. Family workspaces retain Internal, Private, Workspace, and Public while removing Client Visible. The Notes service and access policy enforce the same workspace-type rules, preserve Business publication authority, and normalize legacy inapplicable values only in the access/read projection unless a later explicit write changes them.

As of 0.33.5.18.7.1, Tasks uses the same `slide-out-sidebar` shell for the protected page/read path only. The task list remains the main panel, and the sidebar is limited to filter/navigation controls. Existing task routes, payloads, permissions, list behavior, create/edit modal, checklist editing, timers, recurrence, and bulk actions remain Tasks-owned imperative workflows until their dedicated conversion slices.

As of 0.33.5.18.7.2, the Tasks drawer anatomy is ordered as a non-collapsible `Saved Task Views` selector panel followed by a collapsed `Sorting and Filters` panel. The framework owns that sidebar panel shell, order, and collapsed/static anatomy. Tasks owns the selector values, saved selection, canonical query mapping, detailed filter fields, and Business-only client visibility rules.

As of 0.33.5.18.7.3, the selected Tasks view is a Tasks-owned `task_view` query contract. The framework still owns only the dropdown shell and panel placement. Tasks maps `My Tasks`, `All`, `Unassigned`, `Overdue`, `Due Today`, `Due This Week`, `Completed`, and `Archived` to status, assignment, and user-local due-date predicates in `tasks.service.js`; advanced filters narrow the selected view rather than replacing it, and Reset Filters leaves the selected view intact.

As of 0.33.5.18.7.4, the Tasks main list mounts through the descriptor `tasks-main-list` detail region and the module-owned `tasks.main.list` behavior. The framework owns the slide-out main panel and region placement, while Tasks continues to own row data shaping, dense row presentation, due/status/assignee metadata, row actions, selection state, canonical query construction, and workflow handlers. The sidebar remains filter/navigation-only and must not render task rows or the bulk toolbar.

As of 0.33.5.18.8.1, the Tasks bulk-action toolbar shell uses `LongtailForge.view.createBulkActionToolbar`. The framework owns the collapsed `<details>` shell, summary/title anatomy, selected-count chip placement, and generic toolbar CSS. Tasks owns the selected count value, auto-open-on-selection rule, bulk control fields, action selection, confirmation prompts, API payloads, and refresh behavior.

As of 0.33.5.18.8.2, Tasks wires the existing non-destructive bulk behavior through that framework-owned toolbar shell without moving meaning into the framework. Status, priority, due date, due time, assignee replacement, and tag add/remove/replace payloads are assembled in the Tasks browser adapter and applied through `POST /api/tasks/bulk` / `tasksService.bulkUpdate()`. Permission checks, validation, partial failures, and tag assignment preservation remain service-owned.

As of 0.33.5.18.8.3, Tasks also wires the shipped lifecycle bulk actions through the same toolbar shell. The browser exposes a module-owned Lifecycle selector that offers Archive for selected non-archived tasks and Restore for selected archived tasks. The service delegates to existing `archive()` and `restore()` functions so lifecycle permissions, audit, events, search indexing, and parent-recovery behavior remain service-owned. Tasks still does not expose task delete, soft-delete, or permanent-delete controls in the browser or public API.

As of 0.33.21.11.3, Tasks adds Project assignment to its module-owned bulk controls while preserving the framework-owned toolbar shell. Project is required for this action. Business workspaces may use a Client scope selector to narrow Projects and submit the selected Project's derived Client; Personal and Family workspaces remove that control. `tasksService.bulkUpdate()` delegates each target to the canonical Task update path, keeping old/destination-scope authority, workspace and active-Project validation, Client matching, partial errors, audit/event/tag/Search/reminder effects, and response shaping server-owned.

As of 0.33.21.11.4, the canonical Task editor keeps its primary Save action in place for both Add and Edit while retaining explicit Save & Close and Cancel dismissal. Parent Task options are module-owned hierarchy projections: parents precede descendants, hyphen-prefix indentation mirrors shared project hierarchy labels, and sibling ordering is case-insensitive alphabetical.

As of 0.33.21.11.5, Tasks owns parent-Project cascading as a service/repository contract. A Project change resolves every workspace-scoped descendant, validates each changed Task in its old and destination scope, and commits parent plus descendant Project/Client context in one transaction before emitting per-Task audit, event, tag-propagation, Search, and reminder side effects. Business Client context derives from the destination Project; Personal and Family Tasks remain project-only. The update response includes authoritative refreshed descendant Tasks, including when the same path is entered through bulk Project assignment.

As of 0.33.5.18.8.4, the Tasks main list uses `LongtailForge.view.createListShell()` for the framework-owned list wrapper, status mount, and shared table overflow wrapper while keeping task rows module-owned. Tasks still renders the dense `task-density-row` content, metadata bands, row actions, selection state, and canonical query behavior through its browser adapter. The guardrail inventory reports `tasks.workspace`, but strict declarative enforcement remains limited to `lists.workspace` and `notes.workspace` until a later Tasks strict-conversion slice.

As of 0.33.5.18.9.1, the Tasks add/edit dialog shell renders through `LongtailForge.view.renderDescriptorModalForm()` from a Tasks-owned `task.editor` descriptor. The framework owns the modal/form/footer/action-group/field-grid anatomy, while Tasks owns the field fragments, save payloads, validation, workspace-specific Client visibility, assignment, due/status/priority behavior, recurrence, checklist, timer, tags, files, notes, and notification follow behavior. The recurrence editor remains a Tasks-owned escape hatch until its dedicated preservation slice.

As of 0.33.5.18.9.2, `LongtailForge.tasksDialog.openTaskEditor()` is the canonical browser API for Task editor opens. Tasks page actions, Workbench, and framework module actions use that opener for add/edit flows; callers can pass defaults, source context, per-open save callbacks, and refresh hooks without creating another Task form. Module actions expose the triggering control so the editor can restore focus on close.

As of 0.33.5.18.9.3, the Task editor's primary field grouping uses one framework-owned `Task Details` modal section before specialized task-owned fragments. Tasks still owns the meaning and payloads for status, priority, parent task, due fields, resume note, next action, primary-context rules, description, assignees, and blocked reason. Business workspaces expose nullable Client and Project context, Project can derive Client, and Personal/Family workspaces hide Client while preserving Project context and saving an empty client value. Saved or defaulted context controls must hydrate safe readable labels and must not show raw UUIDs in normal UI. Blocked Reason is hidden and disabled unless Status is `Blocked`.

As of 0.33.21.4.5, the same Tasks-owned editor payload may carry nullable `estimate_minutes`. The server and migration 081 require every non-null value to be a non-negative multiple of 15, while the canonical number control uses matching `min`/`step` hints without becoming the validation authority. Add, edit, duplicate, public API, list/detail/read-model, recurrence-template, and generated-occurrence paths preserve the canonical minute value. Estimate storage does not add scheduling, automatic suggestions, day planning, or settings behavior.

As of 0.33.10.8, the Task editor heading carries a framework-styled `.surface-modal-heading-actions` group holding the icon-only "Open in Workbench" handoff (shared `anvil` icon, edit-mode only, navigating through the canonical `workbench.html?taskId=` deep-link contract) immediately left of the notification bell. As of 0.33.21.4.2, Workbench consumes that parameter once and removes both the canonical `taskId` and encountered legacy `taskID` spelling with `history.replaceState`, preserving unrelated query parameters and hash state before the permission-checked Task read. Empty, unknown, unreadable, cross-workspace, and disabled-Tasks links all return to Focus Selection with the same generic message. Hidden action buttons are framework-correct at the root (`.action-button[hidden] { display: none; }`), and every host that loads the Task editor must load `js/shared/notification-subscriptions.js` so the heading bell is functional rather than ghost-painted — Workbench, Calendar, and Dashboard now do, and the handoff guardrail sweeps every protected view for this pairing.

As of 0.33.5.18.9.6, the Task editor keeps timer and utility behavior as narrow escape hatches inside framework-owned placement. The framework owns the Task Timer section shell, dense action placement, utility footer buttons, and heading bell placement. Tasks and the owning modules keep behavior: task timer rules/routes/service side effects, Tags picker state, Files attachment rules, Notes linked-record panel behavior, task-link copying, and notification follow/unfollow.

As of 0.33.21.4.1, the Task editor's inline reminder override keeps its framework-owned section/grid anatomy while Tasks owns optional secondary-reminder behavior. The second timed and date-only controls each pair an Enabled checkbox with their numeric offset; disabling one omits only that offset from `reminderPolicy`. The existing Tasks reminder service/repository remains authoritative for one- or two-offset persistence, effective-policy reads, occurrence computation, durable job scheduling, and notification delivery.

As of 0.33.5.18.10.1, Tasks row lifecycle actions use descriptor-backed action definitions with registered `tasks.lifecycle.*` behavior IDs. Framework helpers own row lifecycle action-strip placement and disabled button display, while Tasks owns which actions are visible for each task status, the route/update payloads, permission implications, destructive confirmation meaning, service side effects, and canonical list refreshes. This pattern does not add a task delete workflow; archive and restore remain the shipped inactive-history lifecycle.

As of 0.33.5.18.10.2, Tasks row assignment, scheduling, recurrence, and timer workflow actions use descriptor-backed action definitions with registered `tasks.workflow.*` behavior IDs. Framework helpers own workflow-menu placement and disabled button display; Tasks owns canonical editor focus targets for assignment, due date, due time, and recurrence, plus the timer route payloads for start, pause, and resume. Inline row actions must not bypass the canonical Task editor for complex assignment, context, scheduling, or recurrence changes.

As of 0.33.5.18.10.3, Task detail/read summary metadata uses `LongtailForge.view.createDetailBadgeRow()` inside the canonical Task detail/editor modal. The framework owns the reusable detail badge-row anatomy and shared chip-row classes; Tasks owns status, priority, Client/Project, due-date, due-time, and TTC value selection. The task list remains the primary Tasks view, and normal Task detail metadata must use readable labels or safe fallbacks rather than raw IDs.

As of 0.33.5.18.10.4, Task relationship and linked-note display use shared row anatomy without changing ownership. Framework helpers own the row/badge/list structures for task context chips and linked-note rows. Tasks owns parent/child relationship validation, blocking and recovery behavior, route payloads, and relationship summaries. Notes owns the linked-note read model, link/unlink behavior, task-created note context defaults, and safe labels. Normal relationship and linked-note UI must not display raw UUIDs as labels.

As of 0.33.5.18.10.6, Tasks strict declarative guardrails enforce `tasks.workspace` alongside Lists and Notes. `docs/tasks-strict-guardrail-inventory.md` is now the hard-fail contract: Tasks must not rebuild page shell, slide-out sidebar shell, filter panel shell, bulk toolbar shell, modal shell/footer, standard field grids, standard action placement, or framework-owned chrome through raw templates. The documented Tasks-owned escape hatches remain task rows, workflow behavior, bulk semantics, relationships, recurrence internals, checklist behavior, timer state, utility behavior, workspace context, payloads, and validation.

As of 0.33.5.18.10.7, the canonical Task editor opener is the cross-surface Task add/edit contract. `LongtailForge.tasksDialog.openTaskEditor()` remains a Tasks-owned browser helper, while Tasks page actions, Workbench actions, and registered `LongtailForge.moduleActions` dispatch route into that helper with caller defaults, source context, focus return, and refresh hooks. Future Quick Action Center or module-triggered task creation flows should dispatch the registered Task module action or call the opener with safe defaults/context rather than duplicating Task forms, importing Task field internals, or constructing Task save payloads outside the Tasks module.

Descriptor text fields may use either literal display text or terminology keys. Literal fields such as `label`, `title`, and `description` are stable fallbacks. Key fields such as `labelKey`, `titleKey`, and `descriptionKey` reference the resolved module terminology for the current workspace type. The resolver applies `default`, then `personal` for Family workspaces, then the exact workspace type, matching the existing module terminology rules. If a key resolves, it replaces the matching literal field for display; if a key is missing, the literal field remains. Terminology keys affect descriptor display text only. They must not change descriptor IDs, module IDs, view IDs, routes, permission IDs, data-source field bindings, behavior IDs, stored records, or business workflow behavior.

Modules contribute declarative view data, normalized data endpoints, field-binding maps, and named behavior IDs for custom actions. The framework owns common page/layout/modal/table/action anatomy, surface classes, overflow wrappers, responsive behavior, focus/accessibility defaults, terminology resolution, permission-safe app-shell descriptor delivery, descriptor data-source fetching, field-binding projection, default loading/empty/error states, refresh, and descriptor-to-DOM rendering through the existing `LongtailForge.view` primitives. Modules keep ownership of record labels and fields, validation, save payloads, API routes, permission checks, service logic, behavior handlers, and workflow decisions.

Dashboard items require `id`, `label`, `renderer`, and `moduleId`; they may include `description`, `dataRoute`, `placement`, `requiredPermissions`, `requiredWorkspaceCapabilities`, `requiresEnabledModules`, `sortOrder`, and display-only terminology. `placement` is a framework-owned Dashboard region selector and must be one of `pulse`, `attention`, `calendar`, `today`, `main`, `activity`, or `secondary`; omitted placement defaults to `main`. Dashboard contributions should describe module-provided cards, summary sections, empty states, or links while the Dashboard page chrome, Workspace Pulse, setup warnings, region layout, status, and empty states remain framework-owned. Module-specific panel data must hydrate through module-owned `dataRoute` endpoints or registered contribution seams. Dashboard contributions from disabled modules are hidden unless a future contribution explicitly allows historical dashboard reads.

Workbench cards require `id`, `label`, `renderer`, and `moduleId`; they may include `description`, `sourceType`, `listRoute`, `actions`, `requiredPermissions`, `requiredWorkspaceCapabilities`, `requiresEnabledModules`, `defaultCollapsed`, and `sortOrder`. Workbench cards from disabled modules should be hidden. The Workbench page owns layout, filters, loading state, status messaging, and generic action dispatch, while module renderers own record-specific presentation.

Timer sources declare record types that can start or control timers. A timer source requires `sourceType`, `moduleId`, and `label`; it may include `listRoute`, `startRoute`, `pauseRoute`, `finalizeRoute`, `removeRoute`, `requiredPermissions`, and `requiredModules`. First-party timer sources should provide all five lifecycle route descriptors: list active/paused timers, start/resume, pause, finalize into a time entry, and remove/discard. Time Tracking owns active timer persistence and finalization. Source modules expose record context and routes; they do not own duplicate timer engines.

Workbench item sources expose actionable records to the Workbench page. They require `sourceType`, `moduleId`, `label`, and `listRoute`; they may include `requiredPermissions`, `requiredModules`, `filterHints`, and `sortHints`. Source modules should expose dedicated list routes rather than pointing at the aggregate Workbench bootstrap payload. Records should normalize to `source_module_id`, `source_type`, `source_id`, `source_label`, `source_url`, `source`, `title`, `description`, `client_id`, `client_name`, `project_id`, `project_name`, `status`, `priority`, `due_at`, `assignee_ids`, `timer_status`, and `elapsed_seconds`.

Linked Context providers expose selectable records to shared Linked Context pickers. A provider descriptor requires `id`, `moduleId`, `targetType`, `label`, `description`, `provider`, `responseContract`, `requiredReadPermission`, and `requiredPermissions`; it may include `requiredModules`, `requiredWorkspaceCapabilities`, `workspaceTypes`, and terminology. Provider results use the `linked-context-target.v1` response contract in `docs/linked-context-picker-contract.md`. Source modules own target lookup, permission-safe filtering, sorting, safe display labels, secondary labels, source URLs, and Primary Context hints. The framework may list active providers and render provider-supplied labels through the shared Linked Context picker shell, but it must not construct module-specific labels or sort module-owned records itself. As of 0.33.6.15.1, the picker shell may render an optional client-context selector supplied by the consuming module; Notes owns the Business-only client/workspace options, descendant-aware route scoping, and suffix rules, while Personal and Family workspaces must not surface client-context picker controls or client labels.

The Workbench timer payload includes both flat compatibility fields and a nested `source` object with `module_id`, `type`, `id`, `label`, `url`, and `enabled`. Manual timers use `source_type = manual`. Task timers use `source_module_id = tasks`, `source_type = task`, and `source_id = task_id`; they require both Tasks and Time Tracking to be enabled, task read access, and time entry create access. Future Support Ticket timers should use `source_module_id = support-tickets`, `source_type = ticket`, and `source_id = ticket_id`, and should finalize through the same active timer engine into normal time entries with ticket metadata.

The browser Dashboard and Workbench scripts dispatch registered contribution renderers by `renderer` ID. Dashboard built-ins such as `tasks.needs-attention`, `tasks.today-upcoming`, `tasks.calendar`, and `tasks.pressure` live in the framework host, but their data hydrates through module-owned routes declared on the contributions: Tasks summary panels use `/api/tasks/dashboard-summary`, the Tasks calendar panel uses the bounded `/api/tasks/calendar` window through the shared task-calendar helpers, and Time Tracking effort panels use `/api/time-tracking/dashboard/effort-summary`. The Dashboard protected page is a minimal `data-dashboard-host` shell; `public/js/dashboard.js` owns the framework host anatomy, exposes the renderer registration API, renders Workspace Pulse and setup warning regions, and places available panels from `/api/dashboard` contribution metadata rather than static protected-page markup or first-party panel ID checks. As of 0.33.6.13b, Tasks declares `Needs Attention`, `Today / Upcoming`, and compact `Tasks` dashboard panels. The Tasks route owns attention dedupe and ordering across overdue, blocked, due-soon, and task-linked running/paused timer signals; the Dashboard browser renders only the returned rows, metrics, and handoff actions. As of 0.33.6.13c, Time Tracking declares compact `active-timers` and `recent-time` Dashboard panels through `time-tracking.active-timers` and `time-tracking.recent-time`; default Dashboard placement must not render the retired `current-month-billables` or `hours-billables-chart` billing panels. As of 0.33.6.13d, the `main` Dashboard region is the Module Overview grid for compact module cards; module overview cards should show 2-3 safe metrics, at most one suggested/latest row, and one primary handoff rather than full module indexes. The `activity` region is reserved for a future permission-safe activity digest and may render a quiet deferred/empty state until safe body-free rows exist. As of 0.33.6.14.1, Dashboard/Workbench closeout locks the host boundary: Dashboard summarizes state, pressure, and direction, while Workbench owns live action and recovery. Dashboard must not render full task indexes, full report tables/charts, inline editors/forms, browser-rebuilt task ranking or permission logic, unsafe activity rows, warning internals, or Personal/Family Client/billing language. The Time Tracking billing summary route and billing aggregation service remain Reporting-oriented support boundaries, not default Dashboard report cards. The `task-workbench-items` contribution remains a Tasks-owned source route for task options and compatibility, but Workbench no longer renders it as a full task-list renderer.

As of 0.33.10.6, the Dashboard layout has a framework-owned `calendar` region ordered below `pulse` and `attention` and above `today`, and Tasks contributes the `tasks-calendar` panel (`renderer: "tasks.calendar"`, `placement: "calendar"`, gated by `tasks.view`, module-enabled status, and the projects workspace capabilities) that embeds a read-only month view of the bounded task calendar window. The embed renders through the shared `LongtailForge.taskCalendar` helpers (`public/js/shared/task-calendar.js`) — the same period-range math, bounded `/api/tasks/calendar` fetch, and grid rendering the Calendar host uses — so neither host duplicates grid logic, and entries open through the canonical Task editor with an `Open full calendar` handoff to `calendar.html`. The gating happens server-side through the normal contribution pipeline: workspaces with Tasks disabled or users without `tasks.view` never receive the contribution, so the region hides itself. The embed carries a month/week/day segmented view switch (today-anchored; period navigation stays on the Calendar page). This stays within the 0.33.6.14.1 Dashboard boundary (summary and direction, no inline task editing beyond the canonical dialog, no browser-rebuilt permission logic). The same slice retired the Dashboard `reporting` region and the Client/Project `project-summary` reporting-shortcuts panel (an early-Dashboard relic) along with its dedicated `/api/client-projects/dashboard/project-summary` route and service read — the Reporting page and its own `/api/reporting/project-summary` route are unaffected — and Dashboard panels now suppress an inner panel title that merely repeats the region heading (the panel keeps an `aria-label` instead; distinct titles like the Module Overview cards still render).

As of 0.33.21.9, the Dashboard calendar contribution requests only active task statuses. The full Calendar host shares that default and may widen or narrow it through its multi-select; both consumers send the selected status set through `LongtailForge.taskCalendar` to the Tasks-owned server read rather than filtering rows in browser code.

The 0.33.6.14.1 framework-coupling closeout leaves no temporary Dashboard direct coupling from `0.33.6.13a` through `0.33.6.13d`: `src/services/dashboard.service.js` is framework-owned and only reads module contribution metadata through `modulesService.listDashboardPanels`, while module-specific card data hydrates through module-owned routes/renderers. Broader existing framework-service couplings are deliberately deferred outside this branch: Reporting remains assigned to `0.33.12`, and Public API plus tag propagation remain assigned to `0.39.15`.

The documented closeout allowlist for first-party references in otherwise framework-owned code is narrow: `src/core/modules/registry.js` imports bundled module manifests as the static first-party registry; `src/core/app.js` and `src/core/jobs/worker-cli.js` register current module job handlers at process startup; `src/core/record-scope.js` retains the current Client/Project scope helper until the app-wide hierarchy standard; `src/core/client-project-filter-scope.js` now centralizes the shared permission-aware hierarchy resolver used by Tasks/Workbench client/project scoping; `src/core/modules/modules.service.js` retains only core module dependency metadata; `src/services/work-candidate.service.js` retains the current Tasks and Time Tracking candidate-source readers from the existing `0.33.6.3` contract; and `src/services/workbench-task-focus-related-context.service.js` retains selected-task aggregation imports because that service is explicitly selected-task context, not generic Dashboard/Workbench placement. New generic Dashboard/Workbench host decisions should not add first-party module imports or hardcoded module IDs outside that allowlist.

Work resume state is framework-owned recovery infrastructure. Modules do not create their own global resume tables, ranking feeds, browser resume APIs, or Workbench "left off" UI. The framework stores current-user recovery snapshots in `work_resume_state`, exposes protected browser reads through `/api/work-resume`, and keeps public `/api/v1` resume-state routes deferred. Producer modules decide which safe lifecycle events are resumable and shape explicit payload fields through the resume-state producer contract. Read-time access remains authoritative: every returned row must pass a module-owned read resolver, and disabled, missing, deleted, completed, archived, finalized, or permission-denied records stay out of primary left-off results.

Resume-state producer definitions are registered in service code, not in module manifests. A producer declares a stable ID, source module ID, source record type, subscribed safe event names, and a payload builder. Payload builders may return explicit fields such as module/record identity, title, source URL, client/project IDs, last action, last worked timestamp, status, priority, due date, next action, handoff note, blocked reason, rank hint, and sanitized metadata. They must not mine or copy arbitrary bodies, comments, rendered HTML, secure/encrypted fields, attachment internals, protected storage paths, scanner data, or hidden linked-record labels. A producer may return a removal action for source records that should no longer have resume state.

Resume-state read resolvers are module-owned permission checks registered by module/record type. A resolver receives the current session, workspace/user IDs, source record identity, and stored row, then returns whether the current user can still read the source plus optional lifecycle flags such as `deleted`, `completed`, `archived`, or `finalized`. The framework uses those flags for filtering; the resolver remains the source of truth for source-record visibility. First-party initial producers cover Tasks, Lists, Notes, and Time Tracking. Private and secure Notes are excluded from global resume-state rows in this foundation release, and sourced task timers update the source task row while manual active timers remain Time Tracking-owned.

As of 0.33.6.1, the Dashboard/Workbench branch starts from existing framework seams rather than a greenfield contract. The already-shipped contracts are module `dashboard` contributions, module `workbench` contributions, `timerSources`, `workItemSources`, and the framework-owned work-resume producer/read-resolver registry. The net-new 0.33.6 contracts are a focus-mode registry plus one normalized work-candidate model/service layered on those existing seams. Later 0.33.6 slices remove remaining framework hardcoding, including static Dashboard panel markup and deprecated top-level module-setting compatibility flags. Modules should extend the existing contracts instead of inventing alternate Dashboard, Workbench, or resume feeds.

As of 0.33.6.2, `src/services/work-candidate.service.js` is the framework-owned normalization point for work candidates used by next-action and resume behavior. The normalized candidate shape uses camelCase fields for module/record identity, title, context label, reason, primary action descriptor, source URL, priority, due timestamp, blocked reason, and rank hint, with safe compatibility metadata for resume reads. The service assembles candidates from existing resume-state rows and live timer signals gated through the shipped `timerSources` contract; modules do not add a second candidate-source manifest field. Candidate normalization reuses the resume-producer field allowlist and forbidden-field matcher, and nested metadata/action payloads pass through the same scrubber so body text, comments, HTML, secure/encrypted values, attachment internals, storage keys, scanner data, and hidden labels cannot ride along in candidate payloads.

As of 0.33.6.3, ranked work candidates are produced by a pure deterministic ranking helper over normalized candidate fields. Running timers rank first, followed by paused timers, overdue assigned-or-assignment-unspecified work, work due today, blocked or stale work, recently touched work, work due this week, and later work. Ties use only candidate fields: rank hint, due date, priority, last activity time, title, and candidate ID. The `listWorkCandidates()` path filters resume-state rows through active `workItemSources`/`timerSources` contributions before ranking, so Tasks contribute task candidates through their existing work item source and resume producer rows, while Time Tracking contributes running/paused manual timers through the existing timer source plus the thin live-timer pull reader. Lists, Notes Active Work, and future Tickets should join ranked candidate output by becoming ready on those existing seams, not by adding a second candidate-source manifest axis.

As of 0.33.6.4, `src/services/work-focus-modes.service.js` owns the framework focus-mode registry and resolver for Workbench-style candidate curation. The canonical modes are Start my day, Pick up where I left off, What's due next, Work this week, Review blocked work, In progress, Project focus, and Client focus. Each mode resolves to a normalized focus context with a scope, deterministic status/date/rank filters, and a `candidateQuery` that is passed into the shared work-candidate service rather than creating separate pages or module-specific feeds. Client focus is Business-workspace-only; Personal and Family workspace mode lists must not expose Client focus, client scope, or Client labels. Date-focused modes use due-date filters and the shared due-datetime candidate sort rather than rank-bucket filters so recently touched due work is not accidentally hidden by the ranking bucket order.

As of 0.33.21.3.2, Start my day and the ranked fallback for Pick up where I left off read a bounded workspace focus policy from protected framework settings. Administrators may select from four registered candidate groups and three fixed ordering presets; the default reproduces the prior rank order. The resolver, not the browser, maps those values to rank buckets. Running and paused timers remain fixed first, invalid stored values fall back to defaults, Blocked stays excluded outside Review blocked work, and settings cannot alter permission, visibility, scope, lifecycle, or readable-record eligibility.

As of 0.33.6.5, `src/services/workbench.service.js` no longer imports first-party module services or names Tasks/Time Tracking module IDs to build bootstrap data. The Workbench bootstrap returns generic module state keyed by module ID, contribution registry data, and framework-normalized work candidates; rich card payloads are loaded through each active Workbench contribution's module-owned `listRoute`. The Tasks workbench item route is owned by the Tasks module service, while Time Tracking owns the active timer list route. The framework module registry also no longer injects deprecated top-level module flags or special-cases first-party setting IDs when decorating workspace settings.

As of 0.33.6.13a, `views/protected/dashboard.html` is a minimal framework host and `public/js/dashboard.js` builds the Dashboard surface through `LongtailForge.view` primitives. Dashboard panels render from the permission-filtered `dashboardPanels` contribution list returned by `/api/dashboard`, with region placement driven by the validated contribution `placement` field and defaulted to `main` when omitted. `src/services/dashboard.service.js` owns the stable `/api/dashboard` read model, including workspace name/type, framework region metadata, Workspace Pulse, safe setup warnings, and active contribution metadata; it must not import first-party module services or repositories for generic Dashboard decisions. Module-specific data stays on module-owned routes. The retired hidden `data-dashboard-extension-panels` stub, static protected-page billing/project panel markup, host-owned module payloads, and ID-based Dashboard placement logic must not return.

As of 0.33.18.6, the minimal Dashboard host has one native ES-module entry and receives active Dashboard `browserAssets` beside panel metadata. Framework source owns the same-origin, versioned compatibility loader and Dashboard anatomy stylesheet; Tasks and Time Tracking own their renderer scripts and panel styles through manifest declarations. `public/js/dashboard.js` may dispatch registered renderer IDs but must not register or import a first-party module renderer itself. Existing globals are a temporary compatibility boundary, not authorization for new ordered-script dependencies. Workbench is explicitly outside this first wave.

As of 0.33.21.19.4, `public/js/dashboard.entry.js` owns the Dashboard warm-first bootstrap. It loads independent script/style batches with `Promise.all`, reads the near-static `/api/dashboard` manifest through `LongtailForge.cachedFetch` with workspace and release in the cache key, and starts every declared module `dataRoute` in one shared route-keyed promise map before contributed assets finish. The manifest is the only stale-while-revalidate payload in this path; Tasks summary, the Today-anchored bounded Tasks calendar window, and Time Tracking effort summary remain live `no-store` reads. `public/js/dashboard.js`, the shared calendar helper, and the Time Tracking renderer reuse those promises rather than refetching. Tasks keeps its Dashboard renderer as a first-paint asset, but its canonical Task dialog is a lazy interaction dependency loaded through the same versioned bridge only when a calendar task is opened.

As of 0.33.21.19.5, those three module-owned routes are also one measured Dashboard capacity contract. `npm run bench:dashboard` records their warm HTTP medians, uncompressed bytes, statement counts, and the real browser first-fetch gap against the selected Today-anchored seed. `dashboard.hot-endpoint-budgets` enforces conservative HTTP ceilings and near-constant cost under unrelated Task/time-entry growth, while each module keeps its tighter service/repository regression. The framework may schedule and deduplicate these declared reads, but their query shape, permission semantics, payload meaning, and tighter budgets remain module-owned.

As of 0.33.6.6, `views/protected/workbench.html` is a minimal framework host and `public/js/workbench.js` builds the guided Workbench surface through `LongtailForge.view` primitives. The guided entry presents the initial focus questions Pick up where I left off, Start with what's due, Work this week, Review blocked work, and Focus on a project; those questions resolve through the 0.33.6.4 focus-mode service and the shared work-candidate service rather than hardcoded module pages. The Workbench UI highlights the top-ranked candidate first, keeps secondary candidate/card lists subordinate, and uses useful empty states.

As of 0.33.6.6a, the Workbench recommended-action panel gained right-aligned icon-only cycle controls. As of 0.33.6.12c-1, the active recommendation window is the top five ranked candidates, with one shown at a time in the main column. Additional candidates render in the right-side "More in this focus" Inspector overflow list instead of a duplicate main-column task list, and the overflow starts after the top-five window so the recommended cycle and Inspector list do not duplicate records.

As of 0.33.6.6b, the Workbench host no longer renders a standalone top status box or a static intro subtitle under the page heading. The browser host mounts the shared `LongtailForge.view.createStatusMessage()` primitive inside the page-header body, so loading/updating/error messaging and the project-focus empty-context prompt render in the former intro slot without reintroducing custom header/status chrome.

As of 0.33.6.12c-1, Workbench has explicit `focus-selection` and `task-focus` browser states. Primary actions on normalized Task candidates are labeled `Focus task` and enter Task Focus by setting the active task focus instead of opening the Task edit modal. The header action slot is `Change Focus`; it is disabled in Focus Selection, enabled in Task Focus, and returns to Focus Selection without mutating the current focus mode, Client filter, or Project filter. The canonical registered `tasks.edit` module action remains available for explicit context opens and the Task Focus Edit action, passing source context, candidate ID, refresh hooks, status hooks, and focus-return context into `LongtailForge.tasksDialog.openTaskEditor()`. Non-task primary candidate actions use an explicit temporary page fallback until their owning module has a Task Focus-equivalent target view. As of 0.33.21.4.2, a Time Tracking-owned manual active-timer recommendation is labeled `Continue in Time Tracking` and deliberately navigates to `time-tracker.html`; the existing `time-tracking.timer.create` action cannot represent an already-running or paused timer identity, so Workbench does not register or own an active-timer editor or lifecycle opener.

As of 0.33.6.12c-1, Focus Selection repurposes the Workbench right panel as the bounded "More in this focus" candidate overflow surface. Its title clicks choose/focus the candidate through the same primary candidate path used by the recommended-action card. The old main-column "More in this focus" `<details>` section and `workbenchSecondaryCandidates` hooks are retired; Timers remain in the main column below the recommended work area until the later Task Focus timer slice changes timer placement.

As of 0.33.6.12c-1, Task Focus renders a mostly read-only selected-task work surface in the Workbench main column. It hides Focus Selection questions, the recommended-action card, the Focus Selection candidate overflow, and the current timer card; shows an icon-only Edit, Complete, and Block action strip; reads the focused task through `GET /api/tasks/:taskId`; opens Edit through the canonical `tasks.edit` module action; calls `POST /api/tasks/:taskId/complete` for Complete and returns to Focus Selection; and calls the existing `PUT /api/tasks/:taskId` status update path with `{ status: "blocked" }` for Block while staying in Task Focus. Task Details is a read-only collapsed disclosure with safe title, status, due date/time, priority, assignees, Client/Project labels, blocked reason, and description. Task-linked timers and selected-task context Inspector rows remain deferred to their later 0.33.6.12 slices.

As of 0.33.6.12c-2, the Task Focus Checklist section is a check and uncheck only execution surface in the Workbench main column. It renders after read-only Task Details, opens by default when the focused task has checklist items, collapses by default with `Edit task to add checklist items.` when empty, and uses POST `/api/tasks/:taskId/checklist/:itemId/{check|uncheck}` for item toggles. Workbench does not add, remove, rename, or reorder checklist items; those structure edits remain in the canonical Task editor. Checklist permission checks, progress updates, audit records, internal events, notification/search side effects, and refreshed task payloads remain Tasks-owned.

As of 0.33.6.12l, Task Focus checklist toggles are status-aware through that Tasks-owned check/uncheck service path. Checking any checklist item on an Open task returns an In Progress task payload. As of 0.33.21.3.3, unchecking the last checked item returns Open only when Tasks finds no remaining checked item, running or paused task timer, or persisted task-linked time entry. As of 0.33.21.4.3, checking a Blocked Task returns In Progress with an empty active Blocked Reason; unchecking checklist-only work uses the normal no-evidence Open transition and does not restore the old reason. Tasks owns this combined started-work decision; Time Tracking exposes the scoped time-entry existence fact through its public service boundary without transferring status authority. Complete and archived tasks are not rewritten by checklist toggles. Task-level audit, events, search refresh, reminder scheduling, checklist progress, and the returned readable task payload remain service-owned; Workbench only applies the returned authoritative task so status and Blocked Reason refresh without a page reload.

As of 0.33.6.12j, recurring task checklist structure is also Tasks-owned. When the canonical Task editor saves a recurring occurrence with `All Future`, Tasks copies active checklist label/order structure into the recurrence template and eligible future active occurrences; past, completed, and archived occurrences are not rewritten, and checked/completed state remains occurrence-specific. Workbench Task Focus only reads and toggles the resulting occurrence checklist rows.

As of 0.33.6.12n, recurring task linked-note relationship structure is Tasks-plus-Notes owned. When the canonical Task editor saves a recurring occurrence with `All Future`, Tasks stores only active readable linked-note relationship metadata on the recurrence template and asks Notes to materialize normal task-target `note_links` rows on eligible future active occurrences and newly generated instances. Removing a source linked note and saving `All Future` removes only propagated future links for that recurrence template; Notes records, note bodies, manual links, and past/completed/archived task occurrences remain untouched.

As of 0.33.6.12d-1, Workbench timer behavior is split by view state. Focus Selection Timers is an active/paused timer control list only: the manual timer creation row is removed from Workbench and timer creation remains owned by QAC until the Time Tracking create-timer modal ships. Task Focus renders a default-open task-linked Task Timer section after Checklist, uses the selected task context automatically, and exposes Start, Pause, Save Time, and Reset controls. Task Focus and readable task timer cards dispatch task timer mutations through `PUT /api/tasks/:taskId/timer`, `POST /api/tasks/:taskId/timer/finalize`, and `DELETE /api/tasks/:taskId/timer` so Tasks/Time Tracking eligibility, permissions, audit/events/search, elapsed-time calculations, and lifecycle side effects remain owned by the existing services. As of 0.33.21.4.3, Task timer start recovers Open or Blocked Tasks into In Progress, clearing the active reason. The task-sourced timer persists whether it caused the transition plus the prior Blocked reason; Reset restores that state only when Tasks finds no checked checklist item or persisted task time, while Finalize and independently evidenced work keep In Progress. Timer/checklist responses carry the refreshed canonical Task so Workbench renders the authoritative status/reason rather than reconstructing lifecycle state.

As of 0.33.6.12k, Task Focus keeps the secondary timer panel visible as `Other Active Timers`. That panel excludes the focused task's running or paused task timer, keeps other task timers and manual timers eligible, and shows `No other active or paused timers.` when nothing else is active. The focused task's timer appears only in the Task Timer section, where the shared live duration tick updates the counter while running; Workbench no longer renders a duplicate focused-task timer card below those controls.

As of 0.33.21.10.4, the phone-only Workbench layout keeps presentation ownership explicit: Task Focus summary chips wrap below task/context copy, and every `Other Active Timers` card keeps its own source/status chips beneath its title with its duration, context, description, and actions inside the same card. This does not change candidate/timer filtering, source labels, routes, mutations, Tasks lifecycle authority, Time Tracking ownership, or the desktop grid.

As of 0.33.6.12d-2, Time Tracking owns `time-tracking.timer.create` as the shared Create Timer module action. QAC Timer dispatches that action instead of navigating to `time-tracker.html`. The Time Tracking dialog owns Client, Project, optional Task, Description, and Billable controls; manual timer starts write through `/api/active-timers/:timerSlot`, selected task timers write through `PUT /api/tasks/:taskId/timer`, and the dialog signals module-action completion plus host timer-refresh notification after a successful start.

As of 0.33.21.8, Quick Action Capture host refreshes use the framework-owned `LongtailForge.quickActionRefresh` subscription helper. A page or card declares the record types and/or module-action IDs it displays; the helper owns event filtering and unsubscribe lifecycle. Workbench subscribes to `active_timer` / `time-tracking.timer.create` and refreshes only its timer source and card, preserving Task Focus and user disclosure state. Other first-party quick actions remain module/page-owned until a matching stale-host surface is identified.

As of 0.33.21.8, Quick Action Capture host refreshes use the framework-owned `LongtailForge.quickActionRefresh` subscription helper. A page or card declares the record types and/or module-action IDs it displays; the helper owns event filtering and unsubscribe lifecycle. Workbench subscribes to `active_timer` / `time-tracking.timer.create` and refreshes only its timer source and card, preserving Task Focus and user disclosure state. Other first-party quick actions remain module/page-owned until a matching stale-host surface is identified.

As of 0.33.6.12e-1, Workbench exposes a Task Focus related-context service route for one selected task. The read model is service-shaped and permission-aware; it orders linked Notes, task Files, linked Lists, same-project active Tasks, then records with shared direct tags, deduplicating by strongest reason. It uses module/service paths rather than focus-mode candidate overflow and does not expose note bodies, secure payloads, file storage details, scanner internals, or unsafe labels. Rendering remains deferred to 0.33.6.12e-2.

As of 0.33.6.12e-2, Task Focus Inspector renders the selected-task related-context route in the existing right-side Workbench Inspector. It stays visible on wide layouts, defaults open, and exposes a caret collapse control that hides only the scrollable list body. As of 0.33.21.10.3, the same Inspector is reachable at the canonical phone breakpoint through the header's icon-only `Open Inspector` action and shared full-screen drawer lifecycle; it remains omitted at intermediate narrow widths, and its content contract is unchanged. Related rows show service-provided source labels, safe titles, reason/context labels, and badges, then dispatch existing module actions (`tasks.edit`, `notes.view`, `lists.edit`, `files.preview`) or explicit fallback URLs. As of 0.33.6.12m, linked Note rows use the Notes-owned `notes.view` modal first so Markdown stays readable; that modal renders the canonical server `body_html` detail payload and its `Edit` action closes into the existing `notes.edit` editor for the same note. As of 0.33.6.12n, recurring-task propagated linked notes are indistinguishable from normal readable linked notes in this Inspector read model and still dispatch `notes.view`. Task Focus must not use Focus Selection overflow candidates, fetch note bodies for the Inspector, or add an embedded preview pane.

As of 0.33.6.12h, the `Same project tasks` Task Focus Inspector group uses due-date proximity ordering inside that group only: due-today and overdue tasks first, then future due dates nearest-to-farthest, then no-due tasks. Ties stay deterministic through task priority, status, updated-at, title, and stable record identity while preserving the existing Tasks service permission/module/workspace boundaries.

As of 0.33.6.12j, the Task Focus summary card shows the selected task's Client/Project path once and renders summary chips for status, priority, due date/time, and safe direct tags from the existing Tasks read payload. Workbench owns that compact read-only summary presentation; Tasks still owns which task labels, due fields, and direct tags are readable, permission-safe, and returned by the canonical task read route.

As of 0.33.6.6d, the due-focused Workbench modes request the shared `due_datetime` work-candidate sort. "What's due next" orders due candidates by due timestamp ascending, so the oldest overdue work appears before the next upcoming due work instead of falling through to rank hint, context, or title ordering. "Work this week" uses the same sort over the full due-from-today-through-week in-scope candidate list, letting the next-due item become the recommendation while the remaining in-scope work stays available to the Workbench overflow/cycling UI. "Review blocked work" now resolves only genuinely blocked candidates through the normalized blocked status signal; stale-but-not-blocked work is excluded so the existing Workbench empty state appears when no blocked work exists.

As of 0.33.6.13a, due-focused Workbench modes include readable active Tasks-owned work items in addition to resume-state and live-timer candidates, so due work is not hidden merely because a task has no recent resume row. "What's due next" and "Work this week" include overdue active tasks first, then due-today/current-window tasks; "Work this week" no longer applies a today lower-bound that drops overdue work. Project and Client focus modes use the same Tasks-owned candidate source inside the shared descendant-aware client/project scope, with overdue scoped tasks ranked ahead of due-today, upcoming, stale/recent, and no-due scoped work. As of 0.33.21.3.1, due Tasks remain eligible when their status is In Progress, and every built-in focus mode explicitly excludes Blocked candidates except "Review blocked work"; that review mode remains blocked-only and sorts blocked-overdue work before less urgent blocked work. Passive recurring-created task candidates stay suppressed except when overdue or inside the existing near-due recovery window. Focus Selection recommendation cycling and right-side Inspector overflow both render the one service-owned overdue-aware candidate order; browser code must not rebuild due or blocked-exclusion logic.

As of 0.33.21.4.4, a work-item source may provide a sanitized `primary_action` with `type: "module-action"`, a stable registered action ID, and safe params. Workbench dispatches that descriptor through `LongtailForge.moduleActions` and does not import the source module's editor. As of 0.33.21.21.3, Tasks no longer uses this generic seam to promote a completed Task's Next Action: Tasks contributes active Task work items only, and completion never creates a source-specific editor handoff. The generic module-action descriptor contract remains available to sources whose active-work meaning requires it.

As of 0.33.6.14.1, the Workbench focus box still exposes separate client and project focus filters instead of a single Project-focus-only picker. The client filter is hidden outside Business workspaces, changing a client reconciles any now-incompatible project before reloading candidates, and both filters apply to every focus mode through the 0.33.6.4 resolver and shared work-candidate service. These filters now use the shared permission-aware hierarchy scope resolver: selecting a parent client or project includes readable descendant sub-clients/sub-projects and their scoped work, while selecting a leaf still drills down to that one client or project.

As of 0.33.6.14.2, direct client/project filters on Notes list reads and Files browse/File Context target reads use the same shared permission-aware hierarchy scope resolver. Parent client selection includes readable descendant sub-clients plus descendant-project records, parent project selection includes readable descendant sub-project records, project selection remains the narrower direct filter when both are present, unreadable descendants remain excluded, Personal/Family still hide client scope, and Reporting keeps its explicit descendant-rollup behavior instead of inheriting this default.

As of 0.33.6.14.3, direct client/project filters on Workbench, Tasks, Notes, Files, Lists, and Search use the same shared permission-aware hierarchy scope resolver. Parent client selection includes readable descendant sub-clients plus descendant-project records, parent project selection includes readable descendant sub-project records, project selection remains the narrower direct filter when both are present, unreadable descendants remain excluded, Personal/Family still hide client scope, and Reporting keeps its explicit descendant-rollup behavior instead of inheriting this default.

As of 0.33.6.6f, Workbench collapsible sections keep native `<details>`/`<summary>` semantics while exposing a consistent visible caret and synchronized `aria-expanded` state. As of 0.33.6.12c-1, the former main-column "More in this focus" collapsible section is retired in favor of the right-side Inspector overflow. The Timers section still starts open only when active or paused timers are loaded in Focus Selection. As of 0.33.6.12k, Task Focus opens the lower timer panel as `Other Active Timers` even when its filtered list is empty, then respects an explicit user toggle within the current browser session.

As of 0.33.6.7, Pick up where I left off executes its declared resume strategy instead of treating the mode as only a recently touched candidate query. The focus service asks the existing resume-state candidate path first, ranks rows with resume-specific precedence (running timer, paused timer, task with resume note, In Progress task, then priority and stable field tie-breaks), and falls back to ranked candidates only when no resume rows are available. As of 0.33.21.3.1, the focus service also merges permission-shaped live timers into a non-empty resume result, so actual running and paused timers are never hidden merely because resume state exists. Readable task-sourced timers use their Task record identity and enter Task Focus; running task timers precede paused task timers, while manual timers remain Time Tracking candidates. Resume-backed candidates may carry their `resumeStateId` for safe resolver context, but as of 0.33.6.12c-1 Workbench no longer exposes candidate dismissal controls. Permission, disabled-module, deleted-record, terminal-task, and private/secure boundaries still come from source catalogs, the batched Tasks lifecycle/readability check, resume-state read resolvers, and producer allowlists. As of 0.33.21.13, passive recurring-created Tasks are excluded from this mode even when overdue or near due; creation-only Tasks due more than seven days out are suppressed while stronger or nearer work exists, and a final empty-mode pass may surface only non-recurring distant creation-only Tasks. Later resume evidence such as timers, resume notes, or Task updates remains eligible.

As of 0.33.6.12f, Pick up where I left off additionally asks the Tasks-owned Workbench item source for the second-most-recent updated readable active task in the current Client/Project scope, using task `updated_at` ordering. Running and paused timer candidates remain ahead of that boost; the boosted task is inserted before the remaining resume/candidate list and deduplicated by source record. As of 0.33.21.3.1, the boost also honors the normalized excluded-status policy, so a Blocked task cannot be reinserted after the mode has excluded it. Fewer than two eligible tasks produce no boost, and completed, archived, disabled-module, unreadable, private/secure, and out-of-filter tasks are excluded by the existing source and permission gates.

As of 0.33.6.12g, Workbench view-state isolation is strict. Focus Selection renders only focus-question, filter, recommended-action, timer, and overflow-candidate surfaces; Task Focus renders the selected-task surface plus the selected-task Inspector context and, as of 0.33.6.12k, the state-aware `Other Active Timers` panel. Hidden opposite-state panels must leave no visible wrapper card, disabled task-action strip, empty task shell, focus-question card, recommended-action card, or stray focusable controls behind when switching states with `Change Focus`.

As of 0.33.6.7, Workbench no longer renders the full Tasks workbench-items list, task filter bar, task sort control, task-list action buttons, `taskItems` browser state, or empty bootstrap `taskItems` compatibility field. The Tasks workbench contribution may still provide `taskOptions` through its module-owned route for surviving source-owned behavior, but `public/js/workbench.js` does not consume route `items` as an all-tasks index. The focused recommended-action card and right-side "More in this focus" Inspector overflow remain the Workbench task-opening surfaces, and this removal does not change focus-mode resolution, candidate ranking, permission/workspace/module scoping, or active timer behavior. The recommended-action cycle buttons use concise `Previous` and `Next` labels as their icon-only accessible names and tooltips.

As of 0.33.6.10b, Quick Action Capture is a framework-owned app-shell drawer, not a Workbench focus mode or module page. `/api/app-shell/bootstrap` returns permission-filtered `quickActions` descriptors, `public/js/navigation.js` stores them in workspace context, and `public/js/footer.js` mounts the quiet bottom-right drawer on protected pages that load the shared app shell. The drawer owns visibility, Escape/outside-click closing, focus return, temporary-fallback labeling, current-page context capture, and lazy loading needed for modal-backed actions. Task, Note, and List actions dispatch through `LongtailForge.moduleActions` and the existing `tasks.add`, `notes.add`, and `lists.add` openers. Notes and Lists now expose canonical add/edit opener registrations (`notes.add`, `notes.edit`, `lists.add`, `lists.edit`) backed by their existing page-owned editors; the wrappers only prepare dialog data, pass host context, and signal save/cancel. Files exposes attachment-scoped `files.edit` and `files.preview` registrations backed by the existing File Context and File Preview modals. Generic File capture remains an explicit temporary page fallback because there is no existing targetless upload modal to wrap; do not create one inside QAC. As of 0.33.6.12d-2, Timer is no longer a fallback and uses Time Tracking's `time-tracking.timer.create` opener; File capture, Reporting, and Search remain explicitly labeled temporary page fallbacks until their modal/opener contracts ship. The drawer must not add badges, alerts, recommendations, or a second Workbench-style ranking feed.

As of 0.33.32.20, the complete app-shell response is a framework-owned `AppShellBootstrap` contract. Its producing service is checked, and the browser normalizes fetched unknown JSON through the checked `LongtailForge.appShellBootstrap` adapter before `navigation.js` reads it. Modules continue contributing through their registered manifests and catalogs; they must not parse, mutate, or replace the bootstrap envelope, depend on malformed-field fallthrough, or add module-specific branches to the adapter.

As of 0.33.6.11, the Workbench Inspector is a Workbench-only right-side context panel on wide layouts. In Focus Selection it renders from the same permission-safe focus candidates already returned by the Workbench focus service, uses candidate titles/context labels only after raw-ID fallback checks, and opens related Task, Note, and List titles through existing registered module actions. It does not add a new query surface, does not render an embedded preview pane, and does not expose body text, secure/private content, file storage details, or raw IDs as labels. As of 0.33.21.10.3, only the canonical phone layout adds access through the shared full-screen slide-out; intermediate narrow layouts still hide it, and its placement does not compete with the QAC drawer. As of 0.33.6.12e-2, Task Focus Inspector consumes the selected-task related-context route instead of focus candidate overflow.

As of 0.33.6.11b, Workbench no longer renders a Quick Notes section. Quick capture is owned by the QAC Note action, and related note/list/task context is owned by the Workbench Inspector over permission-shaped focus candidates. Do not reintroduce a placeholder note card, note capture form, or note/context feed in the Workbench body unless a future roadmap slice explicitly reopens that surface.

Cross-screen add/edit dialog actions are registered in browser code through `window.LongtailForge.moduleActions.register()`, not through a manifest field. A dialog action must identify its `actionId`, `moduleId`, `recordType`, `mode`, label, required modules, required permissions, and required workspace capabilities, then provide a module-owned `open(params, hostContext)` callback. The framework action dispatcher owns discovery, availability filtering, dispatch, host lifecycle, focus return, and completion callbacks. Modules own dialog rendering, form state, validation, API calls, save behavior, reset behavior, and record-specific permission handling. The dispatcher does not embed module pages, create iframes, or import module-specific form internals. Settings and setting modals are excluded from this action contract and remain in settings pages. Future manifest metadata may declare action descriptors, but browser opener functions should remain module-owned assets rather than framework imports of module form internals.

The Task editor is the first completed workflow example for this pattern: the framework can ask Tasks to create or edit a task from Workbench or future action surfaces, but the canonical Tasks dialog still owns assignment eligibility, scheduling/context rules, recurrence payloads, checklist/timer/utility fragments, validation, and save/refresh behavior.

Settings items require `id`, `label`, and `type`. Supported field types are `boolean`, `toggle`, `text`, `number`, `select`, `multi-select`, `radio`, and `info`. Settings may include `description`, `placeholder`, `options`, `min`, `max`, `step`, `required`, `inputmode`, `requiredPermissions`, `readOnly`, `readOnlyReason`, `disabledReason`, `moduleStatus`, and `visibleWhen`.

`visibleWhen` has the data-only shape `{ settingId, equals }`. It may reference only another setting in the same contribution, its comparison value must match that controller's type, and visibility dependencies must not be self-referential or cyclic. The browser disables and omits a hidden dependent field; the server remains authoritative for validation and persistence.

A setting with `moduleStatus: true` controls the module enablement row through `workspace_modules`; it must be validated and saved by the server through the registry service. Related module options use `moduleStatus: false` and require an explicit server-side settings handler before they can be writable. The browser settings UI renders field definitions and values from the backend `moduleSettings` payload instead of hard-coding first-party module toggles.

`LongtailForge.settingsRenderer` is the single shared browser settings path. It renders titled sections, grouped module sections, fields, and info panels through `LongtailForge.view`, honors setting metadata without special-casing first-party IDs, collects typed values into `moduleSettings`, applies `visibleWhen`, and routes native/API field errors through the field primitive's message channel. The generic module-settings adapter separates catalog entries with `target: framework` into `frameworkSettings` without naming their owner; ordinary module state and values remain beneath `moduleSettings`. `/api/settings` and the app-shell workspace context expose module availability through `enabledModules`, `modules`, and `moduleSettings` rather than deprecated top-level flags such as `timeTrackingEnabled`, `tasksEnabled`, or `taskTimersEnabled`.

`GET /api/settings/catalog` is the browser-facing catalog. It returns the fixed attachment-point list plus contribution sections grouped as `attachments.workspace`, `attachments.user`, `attachments.module[moduleId]`, and `attachments["new-workspace"]`; every delivered field has already passed enabled-module, dependency, workspace-capability, setting-permission, and placement-access filtering, and its current value or descriptor default is hydrated separately from contribution listing. Module lifecycle controls remain visible in the workspace attachment for disabled modules because lifecycle recovery is framework-owned, not an ordinary active-module contribution.

The protected Workspace, User, Tasks, Time Tracking, Files, Workbench, and Developer Example Settings views contain only a `data-settings-host` mount and shared assets. `LongtailForge.settingsHost` constructs framework-owned anatomy through view primitives and exposes `data-settings-attachment` targets; page adapters keep owning API calls, atomic domain saves, and operational readouts while contributed values flow through their owner accessors. Workbench uses a protected framework registration with a module attachment label; it is not a feature-module manifest.

The `/api/settings` save contract accepts a `moduleSettings` object keyed by module ID and setting ID plus a `frameworkSettings` object keyed by registered framework setting ID. The server rejects unknown IDs, read-only fields, invalid value types, invalid select options, unauthorized protected framework writes, writable settings that do not have a server-side handler, and old top-level module setting aliases.

Module settings navigation is derived from registered eligible module protected views. The app shell groups those pages under Settings -> Admin -> Modules; Developer Example demonstrates that a contribution mounted beneath `settings.html` with module-placement settings may use the shared module host without a `-settings.html` URL. The framework owns Admin placement and ordering, while module eligibility still comes from the registry and workspace context.

A new ordinary module setting should require only this manifest contribution. Add a registered persistence handler only when the owning module needs retained-table storage or atomic domain behavior, and add an on-change effect only when a successful changed value must trigger owner-owned behavior. Do not add framework branches to `settings.service.js`, `settings-catalog.service.js`, `settings-host.js`, `settings-renderer.js`, normalizers, or `workspace_settings` for ordinary module-owned values. Module manifests cannot target `framework`, mark a setting `protected`, or collide with a framework-registered setting ID.

Permission descriptors require `id`, `moduleId`, `label`, and `description`; they may include `resource` and `operation`. `requiredPermissions` remains as a compact compatibility list for route/view contribution filtering, while `permissions` is the user-facing contract used for database sync and future permission UI. Startup sync inserts or updates declared permissions and inserts default role mappings without deleting existing role permissions.

Resource definitions require `key`, `moduleId`, and `label`; they may include supported operations such as `read`, `create`, `update`, `delete`, `archive`, `restore`, `assign`, `manage`, and display-only `terminology`. The current permission engine still maps known action prefixes to resource keys, but module manifests now provide the resource contract future modules and permission UI can consume.

Default role permission mappings require `roleId` and `permissions`. They are additive: the framework inserts missing `role_permissions` rows and does not remove existing rows that were granted by migrations, admins, or older versions.

Public API endpoint descriptors require `method`, `path`, and `scope`. API scope descriptors require `id`, `moduleId`, `label`, and `description`; they may include `access` (`read` or `write`) and display-only `terminology`. Legacy string scopes are still accepted by the validator and normalized by the registry. The API key UI reads available scopes from enabled module metadata, so disabled optional module scopes are not offered for new keys. Existing API keys still rely on route-level scope checks and module write guards, so writes to disabled modules remain blocked.

Notification permissions are framework-owned. `notifications.view_own` covers current-user notification reads, `notifications.manage_preferences` covers personal notification preferences, and `notifications.manage_workspace_defaults` covers workspace-level default notification settings. Notification APIs must be recipient/workspace scoped, must re-check target record access before opening a notification target, and must not expose private notifications to workspace admins unless a later version explicitly designs that capability.

Tag permissions are contributed by the first-party `tags` module. `tags.manage` covers workspace tag definition management, `tags.view` covers viewing tag definitions and assignments, `tags.assign` covers adding tag assignments to registered target records, and `tags.remove` covers removing assignments. Tags are classification metadata only; visibility, permissions, billing status, workflow status, and archival state must stay in real fields rather than being inferred from tags.

Taggable type descriptors require `targetType`, `moduleId`, `label`, `description`, `tableName`, `idField`, `labelField`, `workspaceField`, `requiredReadPermission`, and `requiredTagPermission`. They may include `clientField`, `projectField`, `requiredModules`, and display-only terminology. The framework discovers taggable target types from module manifests through `modulesService.listTaggableTypes()` instead of maintaining a permanent hard-coded list. Runtime tag assignments use the declared table and field metadata to verify that the target exists, belongs to the active workspace, and can be scoped for permission checks.

Tag propagation descriptors live in optional `tagPropagation` manifest metadata. A descriptor declares the source module/type, target module/type, relationship resolver ID, workspace field, source read permission, target tag permission, required modules, and timing hints. Resolver IDs are registered in the framework tag propagation registry; manifests should not embed function references. Active propagation rules require their modules and required modules to be enabled. The tag service materializes propagated assignments with source metadata, honors suppressions, emits safe tag assignment/effective-refresh events, and exposes repair tooling without asking consuming modules to duplicate propagation SQL.

The `tags` module contributes browser routes under `/api/tags`. `GET /api/tags`, `POST /api/tags`, `PUT /api/tags/:tagId`, `POST /api/tags/:tagId/archive`, and `POST /api/tags/:tagId/restore` manage workspace tag definitions. `GET /api/tags/assignments` reads direct, propagated, and effective tags for a registered target, `PUT /api/tags/assignments` replaces that target's manual tag set in one save, and `POST /api/tags/assignments/:assignmentId/suppress` suppresses a propagated assignment on the current record. Browser tag reads expose direct/manual tags, propagated tags, system tags, and combined effective tags; existing `tags` arrays remain combined effective tags for compatibility. The module also contributes `tags.html` and the shared browser tag helper for tag chips and reusable pickers. Disabling the `tags` module removes the interface and blocks tag APIs without requiring first-party record modules to know how tag storage works.

Search is a framework-owned service. Modules may declare searchable records in `searchableTypes`, but modules do not own global search routing, browser UI, permission filtering, or search storage. Searchable type descriptors require `recordType`, `moduleId`, `idField`, `titleField`, `summaryField`, `bodyFields`, `workspaceField`, `requiredReadPermission`, and `indexer`. They may include `clientField`, `projectField`, `requiredModules`, `tagsTextField`, `visibilityField`, `recordStatusField`, `sourceLabel`, `label`, `description`, and display-only terminology. The `indexer` value must be a stable string ID resolved through the framework search indexer registry; manifests should not contain direct function references. As of 0.32.9.5, the search service exposes capability discovery, declaration validation, registered searchable type lookup, enabled-workspace active searchable type lookup, permission-safe filter composition, backend-neutral search request shaping including `source`, backend adapter runtime capability reporting, canonical `search_index` metadata, normalized framework search document shaping, formal single-record indexing/removal/re-indexing methods, first module-owned indexers for Tasks, Time Entries, Clients, and Projects, framework-owned Help article indexing, service-owned event synchronization for initial module records, rebuild tooling for workspace/module/local app-wide scopes, adapter-owned SQLite FTS repair, adapter-backed search execution, and a protected browser `GET /api/search` route with result-level permission shaping. Active searchable types are filtered by enabled workspace modules and required module dependencies, so disabled modules may keep declarations without contributing active search types, active rebuild targets, active search request targets, authenticated-shell search target options, or results-page filter options. Help contributes framework-owned search types outside module manifests: Help articles use `record_type = help_article`, `source = Help`, `module_id = framework` for framework articles, and the owning module ID for module-declared articles. `search_index` is the source of truth for searchable-record metadata and stores workspace, module, record type, record ID, title, summary, body, tag text, client/project scope, visibility, record status, source, record timestamps, and indexed timestamp. Search request models must remain adapter-neutral: SQLite FTS syntax belongs in the SQLite adapter, PostgreSQL full-text syntax belongs in a future PostgreSQL adapter, and module declarations should not change for either backend. `visibility` describes search visibility metadata and does not replace record permissions. `record_status` supports active/archived/completed-style filtering without becoming a universal workflow state. `source` is a display/search label and not a permission source of truth. Exact tag filtering uses canonical tag assignments even when `tags_text` is denormalized for text matching or ranking. The SQLite adapter creates `search_index_fts` only when the active SQLite build supports FTS5; otherwise it uses indexed `LIKE` fallback over canonical `search_index` fields. FTS rows are synchronized from framework search service writes, removed through adapter-owned single-record removal, and repairable through adapter-owned rebuild from canonical `search_index`; FTS storage must not become the source of truth for permissions, visibility, workspace scope, module scope, or record lifecycle state. Module mutation services should call the framework sync helper after successful create/update/archive/restore/delete flows when a stable service mutation exists; the helper queues durable `search.index` jobs and logs queue failures without moving indexing side effects into browser code. Rebuild-capable module indexers should return workspace documents when called without a `recordId` in rebuild mode; the framework rebuild service performs normalization, canonical upserts, stale-row removal, inactive type cleanup, dry-run counting, protected active-workspace rebuild jobs, local maintenance rebuilds, and scoped adapter repair. Browser search routes call the framework search service and must not query SQLite FTS or canonical search tables directly. Browser results are pruned after full-text matching against the declared read permission and real result scope, then shaped with snippet, source label, status, optional score, context, tags, and safe target hints without exposing raw indexed body text or denormalized tag text. As of 0.33.5.20.5, `GET /api/search` returns an explicit maximum page size, stable tie-break ordering, opaque next-cursor metadata, and batched Client/Project context enrichment for visible results. The shared authenticated shell owns the lightweight icon-triggered global search entry and submits URL parameters to the framework-owned `search.html` results page. The completed browser search workflow is covered by end-to-end regressions for initial indexed record discovery, record edit re-indexing, stable pagination, permission pruning, Help article search, and UI state hooks. Public API search remains separate roadmap work.

Markdown rendering is a framework-owned content service. Modules may store Markdown source and own record semantics, but they must use `src/core/markdown/markdown.service.js` or a module adapter over it for safe HTML rendering, plain-text extraction, excerpts, source normalization, and safe URL handling. The approved syntax set is CommonMark plus explicitly enabled tables, task lists, and safe underline using the `++text++` token; broad extension bundles, raw HTML, raw underline tags, unsafe links, unsafe image sources, renderer rewrites of saved source, and module-specific ad-hoc parsers remain out of scope. The default render mode is for document-style Markdown and keeps CommonMark soft-line behavior; user-authored note bodies may opt into the explicit `user-authored` render mode so single newlines display as visible line breaks without changing saved Markdown. User-specific link-opening preferences must be applied after safe HTML is injected in the browser and must not alter shared cached rendered HTML. Notes owns note body storage, revisions, wiki-link behavior, secure-note access, client-side new-tab post-processing for absolute external anchors, and the `POST /api/notes/preview` draft preview route that renders through the Notes Markdown adapter in the same user-authored mode as saved note detail reads. Help owns article discovery, metadata, ToC navigation, and active module scoping while rendering article Markdown through the shared document-mode service and indexing Help search text through the same plain-text path. Future Knowledge Base records will own publication status, review workflow, source snapshots, and visibility while consuming the same framework Markdown contract.

File attachments are a framework-owned service. Modules may declare attachable records in `attachableTypes`, but modules do not own file storage, storage keys, scan/quarantine state, download routing, attachment metadata tables, or file lifecycle event emission. Attachable type descriptors require `targetType`, `moduleId`, `label`, `description`, `tableName`, `idField`, `labelField`, `workspaceField`, `requiredReadPermission`, and `requiredAttachPermission`. They may include `clientField`, `projectField`, `requiredRemovePermission`, `allowedFileCategories`, `allowedVisibilityValues`, `maxFilesPerRecord`, `maxFileSizeBytes`, `lifecycleEvents`, `requiredModules`, and display-only terminology. The framework discovers attachable target types from manifests through `modulesService.listAttachableTypes()` and active workspace targets through `modulesService.listActiveAttachableTypes()` / `filesService.listActiveAttachableTypes()`, so disabled modules may keep declarations without contributing active attachment targets. As of the 0.32.13 closeout, storage metadata lives in `files`, record links live in `file_attachments`, file abuse reports live in `file_reports`, core file permissions are `files.view`, `files.upload`, `files.download`, `files.delete`, `files.manage_quarantine`, and `files.manage_workspace_settings`, and the protected local filesystem adapter validates server-generated storage keys stay under the configured data directory. A single stored `files` row may be linked to multiple records, such as a task and a note, through separate active `file_attachments` rows; the active uniqueness rule prevents duplicate links to the same file/module/target context, not legitimate links to different targets. Browser file routes are framework-owned: `POST /api/files`, `POST /api/files/batch`, `POST /api/files/upload`, `POST /api/files/upload/batch`, `GET /api/files/:fileId`, `GET /api/files/:fileId/download`, `POST /api/files/:fileId/delete`, `GET /api/files/attachments`, `GET /api/files/attachments/counts`, `GET /api/files/attachable-targets`, `POST /api/files/attachments`, `POST /api/files/attachments/:fileAttachmentId/remove`, `PATCH /api/files/attachments/:fileAttachmentId/context`, `POST /api/files/:fileId/report`, and the admin quarantine route. As of 0.33.5.20.5, normal Files browse requests to `GET /api/files/attachments` use bounded page sizes, stable sort modes, permission-shaped visible attachment rows, and next-cursor metadata; attachment panels and owning-record mechanics keep the existing Files-owned route semantics. The attachable-target option route is read-only and may return module/target IDs, target IDs, Client IDs, and Project IDs only as internal option values; display labels and context hints must be readable and must not echo protected IDs, storage keys, protected paths, scanner internals, file hashes, secure-note internals, or unreadable target labels. Business workspaces may receive Client filter options, while Personal and Family workspaces must not receive Client option payload values for normal UI responses. Files upload routes accept authenticated JSON/base64 payloads on the compatibility routes and streamed multipart payloads on the normal browser routes, enforce extension allowlists, simple content signature checks, size limits, as of 0.33.5.25.2 workspace and per-user storage quotas, active target checks, workspace ownership, permissions, Files-owned scanner mode selection, and audit/event emission. `NULL` storage quota settings are unlimited; over-quota uploads are rejected without active file rows, attachments, or usable partial local files, and modules must not implement separate quota checks. As of 0.33.5.25.3, streamed uploads validate sampled signatures during the stream where enough bytes exist, rejected-upload cleanup is awaited/logged, and download plus preview-content routes use storage metadata pre-checks before opening read streams. Downloads go through authenticated app routes and are allowed only for available files with passing/non-required scan status, at least one readable active attachment, and an existing stored object; missing stored objects return clean 404 responses without exposing storage internals. As of 0.33.5.25.4, streamed multipart batch uploads record malformed file parts as per-file failures when the request itself remains parseable, while whole-request parser failures stay request-level errors. The active storage adapter contract is `save()`, `saveStream()`, `read()`, `metadata()`, `delete()`, and `health()`; `quarantineFile()` is a database status/lifecycle transition and does not relocate stored objects. File lifecycle events are canonical `file.*` events with sanitized payloads; they must not include raw file contents, unsafe paths, secrets, or scanner internals. Browser screens should use `LongtailForge.fileAttachments.mount()` for streamed batch upload/list/download/remove mechanics, passing module ID, target type, target ID, optional client/project context, accepted categories, and display callbacks. Modules still own record-specific placement and labels. The framework also exposes a simple Files page for workspace browsing and filtered attachment discovery, while actual attachment management stays on owning record screens. See `docs/0.32-module-file-closeout.md` for the 0.32.x boundary review before building Notes.

As of 0.33.5.21.9.3, the Files preview modal implementation lives in `public/js/shared/file-preview.js` and is exposed as `LongtailForge.filePreview` plus the existing `LongtailForge.filesDialog.openFilePreview()` compatibility entry. Files browse, File Context, Notes Files panels, and other shared attachment helper consumers call the same route-backed Preview modal. `public/js/shared/file-attachments.js` may call the shared preview opener for previewable rows and render icon-only Files action buttons, but it must not inline preview content, File Context, Metadata, Inspector behavior, storage controls, scanner internals, or preview content routes.

As of 0.33.5.22.15, the normal shared attachment helper upload path uses `POST /api/files/upload/batch` with streamed multipart `FormData` and ordered per-file results. The legacy JSON/base64 routes `POST /api/files` and `POST /api/files/batch` remain compatibility routes through the 0.33.5.22 storage/scanner branch and are retired no earlier than 0.33.5.23.0 by a later explicit roadmap slice, but normal browser attachment panels should not require `FileReader` or base64 JSON payloads. Streamed upload cancellation, fatal parser errors, storage stream errors, and oversized payloads must fail with bounded response copy and no active file records, attachments, or usable partial local files. Storage provider selection is configuration-owned by Files: `local` remains the default and only bootable provider in 0.33.5.25.1, while `s3` remains a registered S3-compatible adapter scaffold with mocked client proof coverage for `putObject`, `getObject`, `headObject`, `deleteObject`, and safe health. Selecting `LONGTAIL_STORAGE_PROVIDER=s3` fails app and worker startup until a provider-specific client is wired. S3 diagnostics expose only provider id and safe availability in adapter tests and future client-backed paths. No direct/presigned S3 route ships in this branch; any future signed URL exception must be route-designed, permission-checked per object, short-expiring, audit-aware, and absent from normal list/read/preview/upload payloads unless the route contract explicitly returns it for that one request. Scanner mode selection is configuration-owned by Files: `none` resolves queued `file.scan` jobs to `not_required`/`available`, `noop` is an explicit pass-through mode, `clamscan` is an optional executable scanner adapter, and `clamd` is an optional TCP scanner adapter. ClamAV scanner configuration, S3 bucket/endpoint/credential values, storage paths, storage keys, raw scanner output, raw provider responses, hostnames, ports, socket details, and signed URLs must not leak through module payloads, diagnostics UI, lifecycle events, or audit metadata.

Help Center is a framework-owned product documentation surface. Modules may declare help content in `help.sections` and `help.articles`, but modules do not own Help Center routes, browser chrome, search integration, or contribution validation. Section descriptors require `id`, `moduleId`, and `title`; they may include `description`, `sortOrder`, `audience`, `tags`, `requiredPermissions`, `requiredWorkspaceCapabilities`, `requiredModules`, and display-only terminology. Article descriptors require `id`, `moduleId`, `title`, summary or description, and either inline `body` or a safe relative Markdown `contentPath`; first-party product Help should use `contentPath` under the repo-owned `help/` tree. Article descriptors may include `slug`, `sectionId`, `sortOrder`, `audience`, `tags`, `relatedArticleIds`, `requiredPermissions`, `requiredWorkspaceCapabilities`, `requiredModules`, and display-only terminology. IDs must be stable manifest identifiers, article slugs must be URL-friendly, section/article IDs, article slugs, and article content paths must be unique inside the contribution, and article `sectionId` values must reference a section declared in the same block. Framework-owned Help validation also supports articles without a module ID so core app help does not pretend to belong to a disable-able module. Active Help discovery filters disabled modules and required module dependencies. As of 0.33.5.17.6, the framework serves protected `help.html` plus protected `/api/help` and `/api/help/articles/:articleIdOrSlug` routes for active Help Center content, loads Markdown article bodies through the Help service from safe paths under `help/`, exposes ToC-driven browser navigation from `help/toc.md`, renders article Markdown through the shared framework Markdown service, returns explicit `bodyFormat`/`bodyMarkdown` source fields plus safe `bodyHtml`/`bodyHtmlFormat` rendered fields, indexes active Help articles as `help_article` search rows with shared Markdown-derived body text and `source = Help`, and keeps product Help current-state focused on framework, module, and workflow usage. Help search rebuilds re-read active Help content through the Help service, route results back to Help Center article URLs/actions, and stay separate from future Knowledge Base metadata. Knowledge Base authoring remains later roadmap work.

Notification event descriptors require `id`, `moduleId`, `label`, `description`, `defaultEnabled`, and `defaultPriority` (`low`, `normal`, `high`, or `urgent`). They may include `recipientResolver` for a named module/framework resolver or `recipientMode` for a framework-recognized mode such as `actor`, `assignees`, `workspace_admins`, or `explicit_users`. Notification template descriptors require `id`, `moduleId`, `event`, `title`, and `body`; they may include a relative `url` or `recordLinkPattern`. Notification follow target descriptors require `targetType`, `moduleId`, `label`, `description`, and `requiredReadPermission`; they may include an `eventTypes` list to limit which module notification events can be delivered through per-target follows. The framework stores notification records in a workspace-scoped, recipient-specific `notifications` table with module, event, record, status, priority, URL, and metadata fields.

The notification service creates single or multi-recipient records, lists current-user notifications, counts unread records, marks one or all notifications read, dismisses notifications, archives older read/dismissed records, manages per-target follow subscriptions, and decorates target metadata before returning links. Browser notification routes are framework routes under `/api/notifications`; they only return the active user's own notifications in the active workspace. Target links are hidden when the recipient cannot access the target record. Per-target follows are scoped to one workspace, user, module, target type, and target ID; they can override that user's broader event mute for the followed target but do not mutate workspace defaults or other users' preferences.

The authenticated app shell owns the notification bell/dropdown and reads unread counts from `/api/app-shell/bootstrap` plus `/api/notifications/unread-count`. `notifications.html` is a framework-owned protected page for listing, filtering, marking read, dismissing, and configuring notification preferences. User preferences are stored per workspace/user/event type, while workspace defaults are stored per workspace/event type with an enabled flag and priority override. Notification-producing internal events queue `notification.event` jobs; the worker resolves recipients and creates notification records through the notification service so permission checks, module-enabled checks, subscriptions, and preference filtering stay framework-owned.

Audit record type descriptors require `recordType`, `moduleId`, `label`, and `description`. The audit service accepts framework-owned record types plus module-declared record types. Unknown audit record types are rejected unless a caller explicitly sets an unknown-type allowance for a future import/repair path. Audit change types remain framework-owned common values: `create`, `update`, `delete`, `archive`, `restore`, `login`, `logout`, and `settings_change`.

As of 0.33.5.20.5, `GET /api/audit-logs` is a bounded admin/history read with a maximum normal page size, stable created-at ordering, permission-checked workspace scope, filter options, total count, and next-cursor metadata. CSV export keeps its separate capped export path.

Event type descriptors require `event`, `moduleId`, `label`, and `description`; they may include `recordType` and display-only `terminology`. The first active module event descriptors are Tasks events: `task.created`, `task.updated`, `task.completed`, `task.archived`, and `task.restored`.

Lifecycle hooks remain direct functions on `hooks`: `onModuleEnabled`, `onModuleDisabled`, `onModuleInstalled`, `onModuleUpdated`, and `onModuleRepaired`. Event subscriptions live under `hooks.events` as descriptors with `event`, optional `id`, and `handler`.

Internal events are server-side only. Event payloads normalize to `workspace_id`, `actor_user_id`, `module_id`, `record_type`, `record_id`, `previous_value`, `new_value`, `source`, `metadata`, `session`, and `emitted_at`. Hook failures are logged and reported in dispatch results, but they do not throw back into the core save that emitted the event.

The 0.31.17 event bus is deliberately lightweight. It supports search indexing, notification fan-out, future activity feed updates, integrations, webhooks, and background jobs. Module mutation services and framework event hooks may queue durable jobs from internal events, but browser code should not consume the raw internal event bus.

Activity feed and notification summaries are not full activity feed or notification implementations. They are safe summary helpers for future consumers. `eventSummaries` entries require `event` and `moduleId`; they may provide `activity.label`, `activity.summary`, `activity.url`, `notification.title`, `notification.body`, `notification.url`, and `notification.recipientHints`. Summary helpers return human-readable text and safe relative URLs instead of raw event or audit JSON.

Terminology stays separate: Workbench is the user's live workflow desktop, activity feed is a permission-safe historical timeline, audit log is the authoritative admin/security record, and notifications are directed user alerts.

Workspace-type module terminology is display-only. The resolver merges `default`, then `personal` for Family workspaces, then the exact workspace type. Resolved labels may affect module names, navigation, dashboard cards, Workbench cards, empty states, create buttons, record/resource nouns, notification/event display labels, permission display labels, and API scope descriptions, but they must not change module IDs, route names, permission IDs, API scope IDs, audit record types, database tables, stored records, or historical audit/event data.

## Disable Policy

Disabling a module does not delete module data. Disabled modules hide normal navigation, block browser and public API writes through framework guards, stop future background/module behavior when lifecycle hooks exist, and avoid creating new search, tag, or notification records. Disabled data modules may keep historical reads only when `historicalReadAccess` is true.

Modules with `canDisable: false` are treated as core framework modules and cannot be disabled through workspace module state changes. Disabling a module also fails when another enabled module declares it as a `moduleDependencies` requirement.

Module state transitions are audited as `module.enabled` and `module.disabled` records with `record_type = module`. Dependency or validation failures are audited as `module.enable_failed` or `module.disable_failed` when an actor/workspace context is available.

Active timers sourced from disabled modules remain visible in a limited recovery state so time is not stranded.

## Example

```js
const exampleModule = {
  id: "example-work",
  name: "Example Work",
  displayName: "Example Work",
  description: "Example work items for module contract development.",
  category: "core-workflow",
  version: "0.31.10",
  enabledByDefault: false,
  canDisable: true,
  historicalReadAccess: true,
  browserApiRoutes: [exampleRoutes],
  publicApiRoutes: [],
  migrationsDir: null,
  protectedViewsDir: new URL("../../../views/protected/", import.meta.url),
  browserAssetsDir: new URL("../../../public/js/", import.meta.url),
  navigation: [{ label: "Example Work", href: "example-work.html" }],
  protectedViews: [{
    id: "example-work",
    path: "/example-work.html",
    moduleId: "example-work",
    file: "example-work.html",
    requiredPermissions: ["example.view"],
    requiredWorkspaceCapabilities: ["example_work"],
    allowDisabledRead: true,
  }],
  browserAssets: [{
    id: "example-work-script",
    moduleId: "example-work",
    path: "/js/example-work.js",
    type: "script",
    views: ["example-work"],
  }],
  dashboard: [{
    id: "example-summary",
    label: "Example Summary",
    renderer: "example-summary",
    moduleId: "example-work",
  }],
  workbench: [{
    id: "example-items",
    label: "Example Items",
    renderer: "example-items",
    moduleId: "example-work",
    requiredPermissions: ["example.view"],
    requiredWorkspaceCapabilities: ["projects"],
    requiresEnabledModules: ["example-work"],
    defaultCollapsed: false,
    sortOrder: 30,
  }],
  settings: [
    { id: "exampleWorkEnabled", label: "Example Work", type: "toggle", placement: "workspace", moduleStatus: true },
    { id: "exampleMode", label: "Mode", type: "select", placement: "module", default: "simple", options: [
      { label: "Simple", value: "simple" },
      { label: "Detailed", value: "detailed" },
    ], requiredPermissions: ["example.view"] },
  ],
  permissions: [{
    id: "example.view",
    moduleId: "example-work",
    label: "View Example Work",
    description: "View example work records.",
    resource: "example_work",
    operation: "read",
  }],
  defaultRolePermissions: [
    { roleId: "workspace_admin", permissions: ["example.view"] },
  ],
  resourceDefinitions: [{
    key: "example_work",
    moduleId: "example-work",
    label: "Example Work",
    operations: ["read", "create", "update", "archive", "restore"],
    requiredPermissions: ["example.view"],
  }],
  auditRecordTypes: [{
    recordType: "example_work",
    moduleId: "example-work",
    label: "Example Work",
    description: "Example work records and audit history.",
  }],
  requiredPermissions: ["example.view", "example.create"],
  publicApiEndpoints: [{ method: "GET", path: "/api/v1/example-work", scope: "example:read", publicDemoCapability: "api_keys" }],
  apiScopes: [{
    id: "example:read",
    moduleId: "example-work",
    label: "Read Example Work",
    description: "Read example work records through the public API.",
    access: "read",
    publicDemoCapability: "api_keys",
  }],
  eventTypes: [{
    event: "example.created",
    moduleId: "example-work",
    label: "Example Created",
    description: "Emitted after an example work record is created.",
    recordType: "example_work",
  }],
  eventSummaries: [{
    event: "example.created",
    moduleId: "example-work",
    activity: {
      label: "Example Created",
      summary: ({ event }) => `Created ${event.new_value?.title || "example work"}.`,
      url: ({ event }) => `example-work.html?item=${encodeURIComponent(event.record_id || "")}`,
    },
    notification: {
      title: "Example Created",
      body: ({ event }) => `${event.new_value?.title || "Example work"} was created.`,
      url: ({ event }) => `example-work.html?item=${encodeURIComponent(event.record_id || "")}`,
      recipientHints: ["assignees"],
    },
  }],
  hooks: {
    events: [{
      id: "example-task-created",
      event: "task.created",
      handler: async ({ event, module }) => {
        // React to another module's event without interrupting the saved task.
      },
    }],
  },
  timerSources: [],
  workItemSources: [{
    sourceType: "example-item",
    moduleId: "example-work",
    label: "Example Items",
    listRoute: "/api/example-work/items",
    requiredPermissions: ["example.view"],
    requiredModules: ["example-work"],
    filterHints: { supported: ["all", "assigned-to-me"] },
    sortHints: { supported: ["due_at", "priority"] },
  }],
  frameworkDependencies: ["module-access", "permissions-service"],
  moduleDependencies: [],
  seedHooks: [],
  repairHooks: [],
};
```
