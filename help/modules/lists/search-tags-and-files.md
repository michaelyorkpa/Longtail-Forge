Lists are searchable by title, description, type, status, item text, safe linked context, and tag text. Search results remain permission-shaped, and disabled modules do not contribute active list search targets.

Tags classify list records for grouping and retrieval. Tags are not permissions, status, workflow logic, or billing logic. Files attach through the framework file service, so file storage, download permission checks, lifecycle events, and shared attachment behavior stay framework-owned.
